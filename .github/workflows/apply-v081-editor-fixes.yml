name: Apply v0.8.1 editor fixes

on:
  push:
    branches:
      - v0.8-work
    paths:
      - '.github/workflows/apply-v081-editor-fixes.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Sanity check
        run: echo "workflow is valid"
