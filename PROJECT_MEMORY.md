# MD Reader — Project Memory & Source of Truth

> هذا الملف هو المرجع الأساسي للمشروع. يجب قراءته قبل أي تعديل مهم، وتحديثه بعد كل إصدار أو قرار معماري مؤثر.

## 1) الحالة الحالية
- الاسم: **MD Reader**.
- الحزمة الثابتة: `app.mdreader.mobile`.
- الإصدار المستقر المعتمد: **v0.8.1**.
- `versionCode`: **11**.
- Android: minSdk 26، compile/targetSdk 36.
- الواجهة: عربية RTL مع دعم الإنجليزية LTR.
- الحالة: **Stable / معتمد بعد اختبار فعلي على هاتف المستخدم**.
- تم اعتماد v0.8.1 ودمج PR #2 إلى `main` بعد موافقة المستخدم الصريحة.
- فلسفة المشروع: تطبيق خفيف وعملي، محلي قدر الإمكان، بلا إعلانات أو تحليلات أو تتبع، ولا تُضحّى الميزات الشغالة لمجرد تبسيط التنفيذ.

## 2) قواعد هندسية ثابتة
1. لا حلول ترقيعية أو string-patching كمعمارية دائمة.
2. `MainActivity.java` مصدر Java مباشر داخل `app/src/main/...`، ولا توجد patch/reconstruction chain في البناء النهائي.
3. كل إصدار تسليم يمر عبر CI واختبارات smoke وفحص JavaScript وبناء Release والتحقق من APK/AAB.
4. التوزيع يكون Release بمفتاح توقيع ثابت، وليس debug key.
5. الـkeystore وكلمات المرور لا تدخل Git أو `build.gradle`؛ تحفظ خارج المستودع وتُمرر عبر environment/secrets.
6. لا يتغير `applicationId` ولا مفتاح التوقيع بين الإصدارات، ويجب رفع `versionCode` لكل إصدار جديد.
7. أي تغيير في التحرير أو التمرير أو الكيبورد أو القوائم يجب اختباره على جهاز فعلي ومستند طويل قبل الاعتماد.
8. لا يُحذف ML Kit أو Mermaid أو الصوت أو أي وظيفة قائمة بهدف خفض الحجم دون قرار صريح موثق.

## 3) الوظائف الأساسية المعتمدة
- فتح/تحرير/حفظ Markdown + وضع معاينة.
- RTL/LTR حسب المحتوى.
- أدوات Markdown: عناوين، غامق، مائل، strike، code/fenced code، اقتباس، قوائم، task list، روابط، صور، جداول وفاصل أفقي.
- Undo/Redo.
- البحث داخل المستند.
- فهرس العناوين والتنقل السريع.
- الملفات الأخيرة والمفضلة.
- استعادة المسودات.
- حفظ موضع القراءة وحالة التحرير `selectionStart/selectionEnd/scrollY` لكل مستند.
- علامات مرجعية.
- Syntax highlighting وMermaid.
- الترجمة المحلية عبر ML Kit وخيارات AI الموجودة سابقًا.
- القراءة بالصوت المحلي وخيار OpenAI TTS.
- Enter واحد يظهر كسطر جديد في المعاينة (`breaks:true`).
- شريط أدوات التحرير يبقى قابلًا للوصول مع لوحة المفاتيح.

## 4) البحث والاستبدال
- المحرك: `SearchReplaceEngine.java`.
- Literal Unicode وليس مقيدًا بالكلمات أو regex.
- يقبل العربية والإنجليزية والجمل والمسافات والرموز وعلامات Markdown والإيموجي والنص متعدد الأسطر.
- السابق/التالي.
- استبدال النتيجة الحالية.
- استبدال الكل.
- عداد المطابقات.
- خيار مطابقة حالة الأحرف.
- لا يوجد سقف اصطناعي لعدد النتائج.
- آمن عندما يحتوي نص الاستبدال على نص البحث نفسه.
- التعديل يقع على النص الأصلي القابل للحفظ ويحدّث history/dirty/draft/title.

## 5) تجربة التحرير والتمرير
### ما فشل في v0.8.0
- `AdjustableScroller extends Scroller` غيّر velocity نظريًا لكن اختبار الهاتف أثبت أن `EditText` لم يعطِ fling فعليًا مناسبًا؛ رفع الإصبع كان يوقف الحركة تقريبًا، وتغيير 10%–500% لم يكن محسوسًا بالشكل المطلوب.
- هذا التنفيذ **أزيل نهائيًا** ولا يجب إعادته.

### الحل المعتمد في v0.8.1
- `FlingEditText.java` مبني على Android `OverScroller + VelocityTracker`.
- swipe سريع ثم رفع الإصبع = استمرار تمرير حركي حقيقي.
- لمس الشاشة أثناء الحركة = إيقاف فوري للـfling.
- سرعة الـfling قابلة للتعديل من **10% إلى 500%** وتحفظ في SharedPreferences.
- السحب المباشر أثناء ملامسة الإصبع يبقى طبيعيًا؛ النسبة تستهدف الانزلاق بعد الإفلات.
- تم اختبار هذا السلوك فعليًا على هاتف المستخدم واعتماده.

## 6) القوائم والكيبورد
- قائمة الثلاث نقاط والقوائم الطويلة تستخدم `ScrollView` عندما يزيد عدد العناصر، لذلك كل الأزرار قابلة للوصول.
- البحث والاستبدال لم يعد يستخدم panel ثابت بارتفاع 86%؛ النافذة أصبحت بحجم محتواها.
- حقلا البحث والاستبدال مدمجان مبدئيًا ويمكنهما التمدد للنص متعدد الأسطر.
- Dialog يطلب `SOFT_INPUT_ADJUST_RESIZE` حتى لا تغطي لوحة المفاتيح حقول الإدخال.
- تم التحقق من هذه النقاط على الجهاز الفعلي قبل اعتماد v0.8.1.

## 7) الحجم والبناء
### سبب الحجم القديم
APK v0.7 debug كان تقريبًا 65–67 MB لأن ML Kit Translation كان يحمل مكتبات `libtranslate_jni.so` لعدة معماريات داخل APK واحد.

### الحل المعتمد منذ v0.8
- `minifyEnabled true` عبر R8.
- `shrinkResources true`.
- APK مباشر خاص بـ `arm64-v8a` للهاتف.
- AAB منفصل للنشر؛ منصة التوزيع تسلّم ABI المناسب للجهاز.
- ML Kit بقي موجودًا ولم تُحذف ميزة الترجمة.

### نواتج v0.8.1 المعتمدة
- `MD-Reader-v0.8.1-arm64-release.apk`
  - الحجم: **18,216,026 bytes**.
  - SHA-256: `9ed728718b4b26c9c8f71db726ebe419be089ecf379016fe180ea948d0212134`.
- `MD-Reader-v0.8.1-release.aab`
  - الحجم: **56,723,131 bytes**.
  - SHA-256: `eece0cea12fe679589b14c566737a5c8cedafb95599b50659c8f1a1d2de71dbe`.

## 8) CI والتحقق
- آخر CI كامل لـ v0.8.1: GitHub Actions run **34540660807** — النتيجة **success**.
- نجح فيه:
  - direct-source architecture assertions.
  - Markdown transform smoke tests.
  - Translation plan smoke tests.
  - Speech plan smoke tests.
  - Search/replace smoke tests.
  - JavaScript syntax check.
  - ARM64 optimized Release APK build.
  - App Bundle build منفصل.
  - ABI/output verification.
  - artifact upload.
- PR #2 تم تحويله من Draft إلى Ready بعد الاختبار، ثم دُمج إلى `main` بطريقة **squash** بعد موافقة المستخدم.

## 9) التوقيع الدائم
- مفتاح Release ثابت موجود خارج Git وتم تسليمه لصاحب المشروع.
- النوع: PKCS12 داخل `md-reader-release.jks`.
- alias: `mdreader`.
- شهادة SHA-256:
  `3D:7F:96:3D:B1:C9:B5:21:1D:3A:7F:0C:22:7D:20:81:08:47:4B:39:08:02:AF:A6:89:11:33:A0:35:BF:83:5A`
- الصلاحية حتى 2054-01-26.
- يجب استخدام **نفس المفتاح** لكل الإصدارات القادمة.
- من ثبت v0.8.0/v0.8.1 Release بنفس المفتاح يجب أن يحصل على الإصدارات التالية كتحديث مباشر مع رفع `versionCode`.

## 10) تاريخ القرارات المهمة
- v0.7: تحسينات أولية للمعاينة، حالة التحرير، التنقل السريع وشريط الأدوات.
- v0.8.0: تنظيف المعمارية، مصدر مباشر، تقليل الحجم، Release pipeline، Search & Replace، إعداد سرعة التمرير الأولي.
- اختبار v0.8.0 على الهاتف كشف أن scroll speed slider لم يكن يحقق fling فعليًا، وأن نافذة البحث والقائمة الطويلة تحتاج إصلاح UX.
- v0.8.1: `FlingEditText` حقيقي + إصلاح البحث/الكيبورد + قوائم قابلة للتمرير.
- 2026-09-11: المستخدم أكد أن v0.8.1 أصبح تطبيقًا يستحق الاستخدام، ثم أعطى موافقة صريحة لاعتماده. تم دمج PR #2 إلى `main` كنسخة مستقرة.

## 11) قاعدة العمل للإصدار التالي
قبل أي تعديل جديد:
1. قراءة هذا الملف أولًا.
2. العمل من `main` المستقر على فرع ميزة/إصدار جديد.
3. عدم تغيير package أو signing key.
4. رفع `versionCode` و`versionName` عند إصدار نسخة جديدة.
5. الحفاظ على `FlingEditText` ما لم يوجد بديل مثبت أفضل باختبار جهاز فعلي.
6. إضافة/تحديث الاختبارات المناسبة.
7. بناء Release والتحقق من الحجم والتوقيع والـABI.
8. اختبار UX الذي يعتمد على اللمس أو الكيبورد على جهاز فعلي.
9. تحديث هذا الملف بما تم، وما فشل، وما تبقى قبل الدمج.
