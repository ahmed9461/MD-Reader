# MD Reader — Project Memory & Source of Truth

> هذا الملف هو المرجع الأساسي للمشروع. يجب تحديثه مع كل تعديل مهم، قرار معماري، تغيير في البناء، أو ميزة جديدة. إذا تعارضت ملاحظة قديمة مع هذا الملف، يُرجع إلى أحدث قسم زمني هنا ثم إلى الكود والاختبارات.

## 1) هوية المشروع
- الاسم: **MD Reader**
- النوع: تطبيق Android لقراءة وتحرير ملفات Markdown.
- الحزمة: `app.mdreader.mobile`
- الحد الأدنى: Android 8.0 / API 26.
- الهدف الحالي: Android API 36.
- الواجهة: عربية RTL مع دعم الإنجليزية LTR.
- فلسفة المشروع: تطبيق خفيف، محلي قدر الإمكان، بلا إعلانات أو تحليلات أو تتبع، ومناسب لقراءة/تحرير ملفات Markdown الطويلة براحة.

## 2) الوظائف المؤكدة الحالية
- فتح/تحرير/حفظ ملفات Markdown.
- وضع تحرير + وضع معاينة.
- RTL/LTR حسب المحتوى.
- شريط تنسيق Markdown: عناوين، غامق، مائل، قوائم، روابط، اقتباس، كود، تراجع/إعادة وغيرها.
- البحث داخل المستند.
- الفهرس والتنقل بالعناوين.
- Markdown tables.
- Syntax highlighting.
- Mermaid.
- الملفات الأخيرة والمفضلة.
- استعادة المسودات.
- حفظ موضع القراءة.
- العلامات المرجعية.
- الترجمة عبر ML Kit والخيارات المضافة سابقًا.
- القراءة بالصوت المحلي وخيار OpenAI TTS.
- منذ v0.7-work: Enter واحد يظهر كسطر جديد في المعاينة (`breaks:true`).
- منذ v0.7-work: شريط أدوات التحرير يبقى قابلًا للوصول مع الكيبورد عبر `adjustResize`.
- منذ v0.7-work: تنقل سريع داخل المستند + أدوات Markdown إضافية.
- منذ v0.7-work: حفظ حالة التحرير (المؤشر/التحديد/موضع التمرير) لكل مستند.

## 3) قواعد هندسية غير قابلة للتجاوز
1. لا تُحذف ميزة شغالة فقط لتقليل الحجم أو لتسهيل التنفيذ؛ أي تغيير وظيفي مقصود يجب توثيقه أولًا.
2. لا نضيف حلولًا مؤقتة أو fragile string-patching كمعمارية دائمة. الهدف في v0.8 هو تحويل المصدر النهائي إلى ملفات Java عادية داخل `app/src/main/...` وإزالة اعتماد البناء على إعادة تكوين `MainActivity` من أجزاء/patches.
3. كل إصدار قابل للتسليم يجب أن يمر ببناء CI نظيف واختبارات smoke وفحص JavaScript والتحقق من الـAPK/AAB.
4. إصدار التوزيع يجب أن يكون **Release signed** بمفتاح ثابت لا يتغير بين الإصدارات.
5. مفتاح التوقيع وكلمات مروره لا تُخزّن داخل Git أو داخل `build.gradle`; تستخدم secrets/environment variables.
6. بعد الانتقال لأول مرة من debug القديم إلى release-key الجديد قد يلزم حذف النسخة القديمة مرة واحدة بسبب اختلاف شهادة التوقيع. بعد ذلك يجب أن تُثبت الإصدارات الجديدة كتحديث فوق القديمة طالما بقي `applicationId` والمفتاح ثابتين وارتفع `versionCode`.
7. تقليل الحجم يتم دون تكسير الترجمة أو Mermaid أو القراءة بالصوت أو بقية الوظائف.
8. أي تغيير في تجربة التحرير يجب اختباره على مستند طويل، لأن هذا هو سيناريو الاستخدام الأصعب.

## 4) مشكلة الحجم — التشخيص المؤكد
فحص APK v0.7 debug (~65–67 MB) أظهر أن السبب الأكبر هو مكتبة ML Kit Translation الأصلية متعددة المعماريات:
- `lib/x86_64/libtranslate_jni.so` ~16.6 MB
- `lib/x86/libtranslate_jni.so` ~16.5 MB
- `lib/arm64-v8a/libtranslate_jni.so` ~15.6 MB
- `lib/armeabi-v7a/libtranslate_jni.so` ~11.1 MB
- `classes.dex` ~8.1 MB
- `assets/js/mermaid.min.js` ~3.2 MB

الخطة الصحيحة: Release + R8/resource shrinking + App Bundle للنشر + APK مخصص ARM64 للتثبيت المباشر. لا نحذف ML Kit لمجرد تقليل الحجم.

## 5) هدف الإصدار v0.8
### مطلوب من المستخدم
- تحسين حجم التطبيق بشكل ملموس.
- سرعة تمرير قابلة للتعديل في قسم التحرير بمؤشر ونسبة مئوية.
- Release دائم التوقيع بمفتاح ثابت بدل debug.
- بحث واستبدال مرن للنصوص/الجمل/الرموز/الأسطر، مع استبدال الحالي واستبدال الكل، بدون سقف تعسفي لعدد النتائج.
- الحفاظ على كل ما كان يعمل وعدم كسر الميزات القديمة.
- تحديث هذا الملف أثناء التنفيذ وبعده.

### معايير القبول
- [ ] مصدر `MainActivity.java` النهائي موجود بشكل عادي في المستودع، والبناء لا يعتمد على سلسلة patch لإنتاجه.
- [ ] ضبط سرعة تمرير التحرير من واجهة إعدادات واضحة، وتظهر النسبة المئوية مباشرة.
- [ ] القيمة محفوظة بين مرات تشغيل التطبيق وتطبق فورًا على المحرر.
- [ ] البحث والاستبدال يدعم أي نص Unicode متعدد الأسطر، وليس الكلمات فقط.
- [ ] يوجد: التالي/السابق، استبدال النتيجة الحالية، استبدال الكل، وعدّاد مطابقات.
- [ ] الاستبدال لا يعمل داخل المعاينة فقط بصورة مضللة؛ عند التعديل يغيّر النص الأصلي القابل للحفظ ويحدّث حالة dirty/undo بصورة صحيحة.
- [ ] release minification/resource shrinking مفعّلان بعد التحقق من عدم كسر ML Kit أو بقية الوظائف.
- [ ] AAB release للنشر + APK ARM64 release للتثبيت المباشر.
- [ ] توقيع الإصدار يعتمد على مفتاح ثابت محفوظ خارج Git.
- [ ] قياس حجم الناتج النهائي وتوثيقه هنا.
- [ ] CI ناجح بالكامل.

## 6) خطة التنفيذ v0.8
### المرحلة A — تنظيف الأساس
- استخراج `MainActivity.java` النهائي الناتج حاليًا من سلسلة v0.3 → v0.7.
- اعتماده كمصدر عادي في Git.
- تبسيط workflow وإزالة reconstruction/patch chain من مسار البناء الفعلي.
- معالجة مشكلات استعادة موضع التحرير المعروفة: عدم إعادة ضبط المؤشر عند الضغط على تبويب التحرير وهو فعال، وعدم جعل `bringPointIntoView()` يلغي `scrollY` المستعاد.

### المرحلة B — تجربة التحرير
- إضافة `AdjustableScroller`/حل أصلي صغير وواضح للتحكم في سرعة fling دون العبث بحركات اختيار النص.
- واجهة إعدادات سرعة التمرير مع SeekBar ونسبة مئوية، افتراضي 100%، مع نطاق عملي واسع.
- اختبار الملفات الطويلة وحفظ الإعداد.

### المرحلة C — البحث والاستبدال
- إعادة استخدام منطق البحث الحالي حيث يصلح، مع واجهة Replace واضحة.
- مطابقة literal للنص كما أدخله المستخدم (Unicode + مسافات + أسطر + رموز).
- عدم وضع حد اصطناعي على عدد الاستبدالات؛ التنفيذ يكون خطيًا وآمنًا من loops عند الاستبدال بنص يحتوي نص البحث.
- دعم استبدال النتيجة الحالية واستبدال الكل.

### المرحلة D — الحجم والإصدار
- تفعيل `minifyEnabled true` و`shrinkResources true` للـrelease.
- اعتماد ABI-aware distribution: AAB للنشر، وARM64 APK مباشر؛ لا نحشر x86/x86_64 في APK الهاتف.
- تحليل الناتج الفعلي بعد R8 وعدم الاعتماد على تقدير نظري.
- تجهيز signing config من environment variables/Secrets فقط.
- توليد مفتاح release ثابت مرة واحدة، وتسليمه لصاحب المشروع ليحفظه بأمان، ثم استخدامه لجميع الإصدارات المستقبلية.

### المرحلة E — التحقق والتسليم
- smoke tests.
- JavaScript syntax check.
- assemble/bundle release.
- فحص التوقيع والحجم والـABI والموارد.
- تحديث قسم السجل أدناه بالنتيجة النهائية.

## 7) قرارات UX الحالية
- أزرار التنقل والأدوات الإضافية داخل toolbar تكون flat مثل بقية الأدوات، لا بطاقات منفصلة.
- وضع المعاينة يبقى مرجع الإحساس الطبيعي للتمرير، بينما إعداد سرعة التحرير يسمح للمستخدم بتخصيص fling بدل فرض قيمة ثابتة.
- الأدوات المهمة تبقى فوق الكيبورد أثناء التحرير.

## 8) سجل التنفيذ
### 2026-09-11 — بدء v0.8
- تم إنشاء هذا الملف كذاكرة/خطة رسمية للمشروع.
- تم تشخيص سبب الحجم من APK فعلي بدل التخمين: معظم الحجم من `libtranslate_jni.so` لأربع معماريات.
- تم اعتماد اتجاه: عدم إزالة ML Kit، بل توزيع حسب ABI + R8 + AAB.
- تم اعتماد الانتقال من build-time patch chain إلى مصدر Java طبيعي كجزء من v0.8.
- الحالة: **قيد التنفيذ**.

## 9) ما لا يزال مطلوبًا الآن
1. Normalization للمصدر النهائي.
2. إعداد سرعة التمرير بالنسبة المئوية.
3. بحث واستبدال كامل.
4. تحسين release size.
5. release signing ثابت.
6. بناء واختبار وتسليم الملفات النهائية.

## 10) ملاحظات تسليم المفتاح
- ملف keystore هو الأصل الحساس. فقدانه يعني عدم القدرة على إصدار تحديثات بنفس التوقيع عند التوزيع خارج Play App Signing.
- لا يُرفع keystore نفسه إلى Git، ولا تُكتب كلمات المرور في الكود.
- يجب الاحتفاظ بنسختين آمنتين من المفتاح خارج المستودع.
