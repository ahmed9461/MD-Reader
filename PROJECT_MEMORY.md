# MD Reader — Project Memory & Source of Truth

> هذا الملف هو المرجع الأساسي للمشروع. يجب تحديثه مع كل تعديل مهم أو قرار معماري أو تغيير في البناء. عند التعارض يرجع إلى أحدث سجل هنا ثم إلى الكود والاختبارات.

## 1) هوية المشروع
- الاسم: **MD Reader**
- النوع: تطبيق Android لقراءة وتحرير Markdown.
- الحزمة الثابتة: `app.mdreader.mobile`
- الإصدار الحالي: `0.8.0` — `versionCode 10`.
- الحد الأدنى: Android 8.0 / API 26.
- compile/target SDK: 36.
- الواجهة: عربية RTL مع دعم الإنجليزية LTR.
- الفلسفة: خفيف، محلي قدر الإمكان، بلا إعلانات/تحليلات/تتبع، مع الحفاظ على الوظائف وعدم التضحية بها لمجرد خفض الحجم.

## 2) الوظائف الأساسية المحفوظة
- فتح/تحرير/حفظ ملفات Markdown ووضع معاينة.
- RTL/LTR حسب المحتوى.
- أدوات تنسيق Markdown، التراجع/الإعادة، الجداول، الروابط، الاقتباس، الكود، القوائم وقوائم المهام.
- البحث، الفهرس والتنقل بالعناوين.
- Syntax highlighting وMermaid.
- الملفات الأخيرة والمفضلة واستعادة المسودات.
- حفظ موضع القراءة وحالة التحرير والعلامات المرجعية.
- الترجمة عبر ML Kit والخيارات السابقة.
- القراءة بالصوت المحلي وخيار OpenAI TTS.
- Enter واحد يظهر كسطر جديد في المعاينة (`breaks:true`).
- شريط أدوات التحرير يبقى قابلًا للوصول مع الكيبورد عبر `adjustResize`.
- التنقل السريع داخل المستند وأدوات Markdown الإضافية.

## 3) قواعد هندسية ثابتة
1. لا تحذف ميزة شغالة لتقليل الحجم أو لتسهيل التنفيذ إلا بقرار موثق.
2. لا توجد سلسلة patch/reconstruction في مسار البناء النهائي. `MainActivity.java` مصدر Java عادي مباشر داخل `app/src/main/java/...`.
3. كل إصدار تسليم يمر ببناء CI، smoke tests، فحص JavaScript، وفحص نواتج APK/AAB.
4. التوزيع الحقيقي يكون Release بمفتاح توقيع ثابت، وليس debug key.
5. الـkeystore وكلمات المرور لا تدخل Git أو `build.gradle`؛ Gradle يقرأ بيانات التوقيع من environment/secrets فقط.
6. ثبات `applicationId` + نفس مفتاح التوقيع + زيادة `versionCode` شرط قبول Android للتحديثات اللاحقة.
7. تقليل الحجم لا يكسر ML Kit أو Mermaid أو الصوت أو أي وظيفة قائمة.
8. تجربة التحرير، خصوصًا التمرير، يجب التحقق منها على مستند طويل وعلى جهاز فعلي أيضًا.

## 4) تشخيص الحجم والحل المعتمد
APK v0.7 debug كان تقريبًا 65–67 MB. السبب الأكبر كان ML Kit Translation لأنه ضم مكتبات `libtranslate_jni.so` لأربع معماريات في APK واحد:
- x86_64 ~16.6 MB
- x86 ~16.5 MB
- arm64-v8a ~15.6 MB
- armeabi-v7a ~11.1 MB
- classes.dex ~8.1 MB
- Mermaid asset ~3.2 MB

الحل في v0.8:
- Release optimization بواسطة R8 (`minifyEnabled true`).
- `shrinkResources true`.
- APK مباشر خاص بـ `arm64-v8a` بدل حشر جميع ABIs في ملف الهاتف.
- AAB منفصل للنشر بحيث منصة التوزيع تسلم ABI المناسب لكل جهاز.
- ML Kit بقي موجودًا ولم يتم حذفه.

### نتيجة الحجم الفعلية
- ARM64 APK قبل التوقيع: **18,182,663 bytes**.
- ARM64 APK النهائي بعد التوقيع: **18,216,026 bytes** (~17.37 MiB / 18.22 MB decimal).
- AAB قبل التوقيع: **56,693,140 bytes**.
- AAB النهائي بعد التوقيع: **56,720,673 bytes**.
- مقارنةً بـ ~67 MB للـdebug القديم، APK الهاتف الجديد أصغر بحوالي **72.8%** تقريبًا.
- فحص محتوى APK النهائي أكد وجود `lib/arm64-v8a/libtranslate_jni.so` فقط وعدم وجود x86/x86_64/armeabi-v7a داخله.

## 5) ميزات v0.8 المنفذة
### أ) سرعة تمرير التحرير
- `AdjustableScroller` صغير ومباشر مبني فوق Android `Scroller`.
- النطاق: **10% إلى 500%**.
- الافتراضي: **100%**.
- النسبة تظهر مباشرة في واجهة الإعداد.
- التغيير يطبق فورًا ويحفظ في SharedPreferences بين مرات التشغيل.
- ضبط السرعة يستهدف سرعة الـfling/التمرير الحركي دون استبدال نظام تحديد النص أو حركات المؤشر.

### ب) البحث والاستبدال
- محرك مستقل: `SearchReplaceEngine.java`.
- Literal Unicode matching، وليس بحث كلمات فقط.
- يقبل العربية والإنجليزية والجمل والمسافات والرموز وعلامات Markdown والإيموجي والنص متعدد الأسطر.
- لا يوجد سقف اصطناعي لعدد المطابقات.
- السابق/التالي.
- استبدال المطابقة الحالية.
- استبدال الكل.
- عداد المطابقات.
- خيار مطابقة حالة الأحرف.
- آمن عندما يحتوي نص الاستبدال على نص البحث نفسه؛ لا يدخل في loop.
- التعديلات تقع على نص المحرر الأصلي القابل للحفظ وتحدّث history/dirty/draft/title.

### ج) استعادة موضع التحرير
- عدم إعادة ضبط موضع المؤشر عند الضغط على تبويب التحرير وهو فعال أصلًا.
- استعادة `selectionStart/selectionEnd/scrollY` لكل مستند.
- تجنب جعل `bringPointIntoView()` يلغي `scrollY` المحفوظ عندما توجد حالة تحرير صريحة.

### د) تنظيف المعمارية
- المصدر النهائي أصبح مباشرًا في `app/src/main/...`.
- workflow النهائي لا يعيد تركيب `MainActivity` من أجزاء أو patches.
- بقي workflow واحد فقط للإصدار الحالي: `build-apk.yml`.
- حذفت workflows الانتقالية بعد انتهاء دورها.
- حذفت `.ci` و`.src-v03` و`.src-parts` وسلسلة patches/source chunks القديمة من فرع v0.8.
- حذفت `apply_editor_ux_v07.py` و`fix_editor_ux_v07_blocks.py` لأن البناء لم يعد يعتمد عليهما.
- أبقينا فقط أدوات الاختبار والأصول المطلوبة للبناء الحالي.
- README تم تحديثه ليتوافق مع v0.8 ومعمارية البناء الحالية بدل وصف debug القديم.

## 6) البناء والإصدار
### Gradle
- `versionName '0.8.0'`.
- `versionCode 10`.
- R8/resource shrinking مفعّلان للـrelease.
- APK الهاتف يبنى مع `-PmdReaderDirectArm64=true`.
- AAB يبنى في Gradle invocation منفصل بدون ABI split.

### لماذا APK وAAB يبنيان منفصلين؟
Android Gradle Plugin رفض الجمع بين multi-APK ABI outputs وApp Bundle مع resource shrinking في build graph واحد بسبب تعدد ملفات `shrunk-resources`. الحل المعتمد فصل العمليتين، وليس تعطيل shrinking أو حذف مكتبة.

### آخر بناء تأكيدي كامل
GitHub Actions run: **34538180306** — النتيجة: **success**.
Commit المختبر: `50902a78bf23e84380a5e58bd88b3a5cb5d4b7a7`.
نجح فيه:
- direct-source architecture assertions.
- Transform smoke tests.
- Translation plan smoke tests.
- Speech plan smoke tests.
- Search/replace smoke tests.
- JavaScript syntax check.
- ARM64 optimized release APK build.
- App Bundle build المنفصل.
- ABI/output verification.
- artifact upload.

التغييرات اللاحقة لذلك commit كانت تنظيفًا إضافيًا لمخلفات مصدر قديمة وتحديث README/PROJECT_MEMORY فقط؛ لم تغيّر كود التطبيق أو Gradle أو workflow المبني.

## 7) التوقيع الدائم
- مفتاح Release ثابت تم إنشاؤه وتسليمه لصاحب المشروع خارج Git.
- النوع الفعلي: PKCS12 داخل ملف `md-reader-release.jks`.
- alias: `mdreader`.
- في PKCS12 الحالي كلمة مرور المفتاح هي نفس كلمة مرور الـkeystore؛ التفاصيل السرية موجودة فقط في حزمة المفتاح الخاصة بصاحب المشروع.
- شهادة SHA-256:
  `3D:7F:96:3D:B1:C9:B5:21:1D:3A:7F:0C:22:7D:20:81:08:47:4B:39:08:02:AF:A6:89:11:33:A0:35:BF:83:5A`
- الصلاحية حتى 2054-01-26.
- APK النهائي تم التحقق منه بواسطة Android `apksigner`: v2=true وv3=true، signer واحد، RSA 4096-bit، والبصمة مطابقة.
- AAB النهائي تم توقيعه بنفس المفتاح وتحقق `jarsigner` منه بنجاح.
- تم التحقق بعد التوقيع أن payload المعبأ للتطبيق لم يتغير؛ أضيفت فقط بيانات التوقيع.

### قاعدة التحديثات
قد يلزم حذف نسخة debug القديمة **مرة واحدة فقط** بسبب اختلاف شهادة debug عن شهادة release الجديدة. بعد تثبيت v0.8.0 الموقعة، الإصدارات التالية يجب أن تستخدم نفس المفتاح ونفس package مع رفع versionCode، وعندها تثبت كتحديث فوق النسخة السابقة.

## 8) بصمات ملفات التسليم النهائية
- `MD-Reader-v0.8.0-arm64-release.apk`
  - SHA-256: `c21167a87f51b68c99491bba49c69267141abd7d7491f2ffff3a36e370b29de9`
- `MD-Reader-v0.8.0-release.aab`
  - SHA-256: `3ddecc8e802c18317a7d09baf8e2a6ebb204606f98a2bbbc2b1ca792c3b4d4cc`

## 9) معايير قبول v0.8
- [x] `MainActivity.java` مصدر مباشر والبناء لا يعتمد على patch chain.
- [x] واجهة سرعة التمرير تعرض النسبة.
- [x] سرعة التمرير تحفظ وتطبق فورًا.
- [x] البحث والاستبدال يدعم Unicode متعدد الأسطر والرموز بلا تقييد للكلمات.
- [x] السابق/التالي + استبدال الحالي + استبدال الكل + عداد.
- [x] الاستبدال يعدل المصدر القابل للحفظ ويحدّث حالة التحرير.
- [x] R8 وresource shrinking مفعّلان.
- [x] APK ARM64 + AAB منفصلان.
- [x] مفتاح Release ثابت وتسليم خاص خارج Git.
- [x] APK النهائي موقّع ومتحقق منه.
- [x] AAB النهائي موقّع ومتحقق منه.
- [x] قياس الحجم الفعلي موثق.
- [x] CI تأكيدي كامل ناجح بعد تنظيف patch-chain.
- [ ] اختبار الإحساس الفعلي للتمرير على هاتف المستخدم بعد تثبيت APK؛ هذا لا يمكن محاكاته بالكامل في CI بلا لمس جهاز حقيقي.

## 10) حالة المشروع الآن
- الحالة: **v0.8.0 جاهز للتجربة على الجهاز**.
- لا توجد مشكلة بناء معلومة في النواتج التي تم توقيعها.
- أهم تحقق يدوي الآن: فتح مستند Markdown طويل، تجربة 50% / 100% / 200% / 500% للتمرير، وتجربة بحث واستبدال على نص عربي/إنجليزي ورموز ونص متعدد الأسطر.
- لا يدمج الفرع في `main` قبل تجربة المستخدم إذا كنا نريد إبقاء main كنسخة مستقرة.

## 11) قاعدة العمل للإصدار التالي
قبل أي تعديل جديد:
1. قراءة هذا الملف.
2. عدم تغيير package أو signing key.
3. رفع `versionCode` عند إصدار نسخة جديدة.
4. إضافة الاختبارات المناسبة للميزة.
5. بناء Release والتحقق من الحجم/التوقيع/ABI.
6. تحديث هذا الملف بما تم وما تبقى.
