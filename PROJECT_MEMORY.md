# MD Reader — Project Memory & Source of Truth

> هذا الملف هو المرجع الأساسي للمشروع. يجب قراءته قبل أي تعديل مهم، وتحديثه بعد كل إصدار أو قرار معماري مؤثر.

## 1) الحالة الحالية
- الاسم: **MD Reader**.
- الحزمة الثابتة: `app.mdreader.mobile`.
- الإصدار المستقر المعتمد على `main`: **v0.11.0**.
- `versionCode`: **14**.
- تم اعتماد طبقة HTML الآمنة وأدوات التحرير بعد CI ناجح وتجربة النسخة الموقعة على الهاتف، ثم دُمج PR #6 إلى `main` بطريقة squash في 2026-09-18.
- Android: minSdk 26، compile/targetSdk 36.
- الواجهة: عربية RTL مع دعم الإنجليزية LTR.
- الحالة: **Stable / معتمد بعد CI ناجح واختبار فعلي على هاتف المستخدم**.
- تم اعتماد v0.9.0 ودمج PR #4 إلى `main` بطريقة squash بعد موافقة المستخدم الصريحة.
- تم اختبار نسخة v0.10.0 الموقعة على الهاتف كتحديث فوق النسخة السابقة، وأكد المستخدم أن التطبيق يعمل بكل سلاسة ثم أعطى موافقة صريحة على الدمج عبر PR #5.
- تم اختبار نسخة v0.11.0 الموقعة كتحديث فوق v0.10.0، وأكد المستخدم أنها ممتازة وتعمل كما يريد، ثم أعطى موافقة صريحة على الدمج عبر PR #6.
- squash merge commit لـ v0.11.0: `037bd3e68d542512415fbe636bbe175831e01b23`.
- فلسفة المشروع: تطبيق خفيف وعملي، محلي قدر الإمكان، بلا إعلانات أو تحليلات أو تتبع، ولا تُضحّى الميزات الشغالة لمجرد تبسيط التنفيذ.

## 2) قواعد هندسية ثابتة
1. لا حلول ترقيعية أو string-patching كمعمارية دائمة.
2. `MainActivity.java` مصدر Java مباشر داخل `app/src/main/...`، ولا توجد patch/reconstruction chain في البناء النهائي.
3. كل إصدار تسليم يمر عبر CI واختبارات smoke وفحص JavaScript وبناء Release والتحقق من APK/AAB.
4. التوزيع يكون Release بمفتاح توقيع ثابت، وليس debug key.
5. الـkeystore وكلمات المرور لا تدخل Git أو `build.gradle`؛ تحفظ خارج المستودع وتُمرر عبر environment/secrets.
6. لا يتغير `applicationId` ولا مفتاح التوقيع بين الإصدارات، ويجب رفع `versionCode` لكل إصدار جديد.
7. أي تغيير في التحرير أو التمرير أو الكيبورد أو القوائم يجب اختباره على جهاز فعلي قبل الاعتماد.
8. لا يُحذف ML Kit أو Mermaid أو الصوت أو أي وظيفة قائمة بهدف خفض الحجم دون قرار صريح موثق.
9. العمل على أي إصدار جديد يبدأ من `main` المستقر وعلى فرع منفصل، ولا يُدمج قبل موافقة المستخدم الصريحة.
10. لا نعتمد على سلوك checkbox الأصلي في Android WebView إذا كان يسبب عرضًا غير متناسق؛ المعاينة يجب أن تكون حتمية بين الإصدارات المدعومة.

## 3) الوظائف الأساسية المعتمدة
- فتح/تحرير/حفظ Markdown + وضع معاينة.
- RTL/LTR حسب المحتوى.
- أدوات Markdown: عناوين، غامق، مائل، strike، code/fenced code، اقتباس، قوائم، task list، روابط، صور، جداول وفاصل أفقي.
- Undo/Redo.
- البحث داخل المستند.
- Search & Replace literal Unicode غير محدود بعدد نتائج اصطناعي.
- فهرس العناوين والتنقل السريع.
- الملفات الأخيرة والمفضلة.
- استعادة المسودات.
- حفظ موضع القراءة وحالة التحرير `selectionStart/selectionEnd/scrollY` لكل مستند.
- علامات مرجعية.
- Syntax highlighting وMermaid.
- الترجمة المحلية عبر ML Kit وخيارات AI الموجودة سابقًا.
- القراءة بالصوت المحلي وخيار OpenAI TTS.
- Enter واحد يظهر كسطر جديد في المعاينة (`breaks:true`).
- شريط أدوات التحرير يبقى قابلًا للوصول مع لوحة المفاتيح.
- دعم task-list موثوق لصيغ `- [x]`, `-[x]`, `- [ x ]`, `* [X]`, `+ [ ]` داخل المعاينة.
- الإجابة المحددة تُعرض بعلامة `✓` واضحة وتظليل أخضر متوافق مع الوضعين الفاتح والداكن وRTL/LTR.
- الأسطر التي تبدأ بـ `✅` تحصل على تمييز الإجابة الصحيحة دون تفعيل HTML داخل Markdown.
- أدوات تحرير مباشرة: **✅ إجابة صحيحة**، **☐ اختيار غير محدد**، **☑ قائمة مهام**، **🧹 توحيد مربعات الاختيار**.

## 4) تجربة التحرير والتمرير
- التنفيذ القديم `AdjustableScroller extends Scroller` فشل عمليًا في إعطاء fling محسوس وأزيل نهائيًا.
- الحل المعتمد منذ v0.8.1 هو `FlingEditText.java` باستخدام `OverScroller + VelocityTracker`.
- swipe سريع ثم رفع الإصبع = استمرار تمرير حركي حقيقي.
- لمس الشاشة أثناء الحركة = إيقاف فوري للـfling.
- سرعة الـfling قابلة للتعديل من **10% إلى 500%** وتحفظ في SharedPreferences.
- تم اختبار هذا السلوك فعليًا على الهاتف واعتماده.
- في v0.10.0 أضيف debounce للأعمال الثقيلة في الملفات الكبيرة، وخُفض حد snapshots للـUndo في الملفات الكبيرة إلى 20 لتقليل ضغط الذاكرة.

## 5) القوائم والكيبورد
- القوائم الطويلة تستخدم `ScrollView` بحيث تبقى كل الأزرار قابلة للوصول.
- نافذة البحث والاستبدال بحجم محتواها وليست panel ثابتة الارتفاع.
- حقلا البحث والاستبدال يدعمان نصًا متعدد الأسطر.
- Dialog يستخدم `SOFT_INPUT_ADJUST_RESIZE` لتقليل تغطية لوحة المفاتيح للحقول.

## 6) GitHub Markdown Import — v0.9.0+
- الملف المسؤول: `GitHubMarkdownSource.java`.
- يوجد زر **تنزيل ملف من GitHub** في الصفحة الرئيسية وخيار مقابل في قائمة الثلاث نقاط.
- يقبل روابط GitHub العامة بصيغة `https://github.com/<owner>/<repo>/blob/<ref>/<path>.md`.
- يقبل أيضًا روابط `raw.githubusercontent.com` المباشرة.
- خيار **تنزيل وفتح** يحمّل الملف ويفتحه مباشرة داخل MD Reader.
- خيار **تنزيل وحفظ في الجهاز** يحمّل الملف ثم يفتح Android document picker لحفظ نسخة محلية.
- التنزيل يتم خارج UI thread حتى لا تتجمد الواجهة.
- HTTPS فقط.
- لا تُرسل Tokens أو بيانات دخول.
- إعادة التوجيه مقيدة إلى GitHub و`raw.githubusercontent.com`.
- المستودعات الخاصة غير مدعومة في v0.9.0/v0.10.0؛ لا تُضاف Tokens يدويًا داخل التطبيق دون تصميم مصادقة آمن.
- الرابط المرجعي الذي اختُبر أثناء التطوير: `https://github.com/ahmed9461/GitDock/blob/main/CHANGELOG.md`.
- تم اختبار الميزة فعليًا على هاتف المستخدم، وأكد أنها تعمل بنجاح قبل الاعتماد.

## 7) الحجم والبناء
- البناء Release يستخدم R8 (`minifyEnabled true`) و`shrinkResources true`.
- APK المباشر مخصص لـ `arm64-v8a` لتجنب حمل مكتبات معماريات غير مطلوبة.
- AAB يبنى بشكل منفصل للنشر متعدد المعماريات.
- ML Kit Translation بقي موجودًا.

### ناتج v0.9.0 المختبر على الجهاز
- `MD-Reader-v0.9.0-arm64-release.apk`
- الحجم: **18,232,410 bytes**.
- SHA-256: `6fd4edd298db3d1a272145b8a12fe63a1ccb2227366bedbb1923141cf7b4f681`.
- موقّع بنفس مفتاح Release المستمر، ولذلك يثبت كتحديث فوق الإصدارات السابقة الموقعة بنفس المفتاح.

### ناتج v0.10.0 المتحقق منه في CI
- `MD-Reader-v0.10.0-arm64-release-unsigned.apk` — **18,200,803 bytes**.
- SHA-256: `d43efea53a183e151e4ccc0c3e7538ac97c966e49e6ad105d60ac415c1803ab7`.
- `MD-Reader-v0.10.0-release-unsigned.aab` — **56,709,179 bytes**.
- SHA-256: `84d0be670af17182a5aa106fa16dcbe8536ad7c1efdac99a60713a9fd00e27f9`.
- تم تجهيز نسخة APK موقعة بنفس مفتاح Release الدائم، وتثبيتها فعليًا كتحديث فوق v0.9.0 على هاتف المستخدم بنجاح.

## 8) CI والتحقق
### v0.9.0
- GitHub Actions Run #67: id `34655147435`.
- المحاولة الأولى نجحت في الاختبارات والبناء والتحقق من APK/AAB، وفشلت فقط عند رفع Artifact بسبب Actions Storage quota.
- بعد جعل المستودع عامًا مؤقتًا، أُعيد تشغيل الـrun.
- المحاولة الثانية انتهت **Success بالكامل**.

### v0.10.0
- GitHub Actions Run #80: id `35131485730` — **Success** بالكامل في التحقق من الكود والبناء.
- GitHub Actions Run #81: id `35133152178` — **Success** بالكامل أيضًا بعد تحديث التوثيق.
- نجحت فيهما فحوص direct-source architecture، Java smoke tests، JavaScript syntax، task syntax + large-document compatibility، بناء ARM64 Release APK، بناء AAB، وفحص ABI/output.
- بعد تحويل المستودع إلى **Public** وإعادة تشغيل الـjob، نجح رفع Artifact أيضًا باسم `MD-Reader-v0.10.0-arm64-unsigned`، وانتهت مشكلة Artifact quota التي كانت تمنع التسليم.

## 9) التوقيع الدائم
- مفتاح Release ثابت موجود خارج Git وتم تسليمه لصاحب المشروع.
- النوع: PKCS12 داخل `md-reader-release.jks`.
- alias: `mdreader`.
- شهادة SHA-256:
  `3D:7F:96:3D:B1:C9:B5:21:1D:3A:7F:0C:22:7D:20:81:08:47:4B:39:08:02:AF:A6:89:11:33:A0:35:BF:83:5A`
- الصلاحية حتى 2054-01-26.
- يجب استخدام **نفس المفتاح** لكل الإصدارات القادمة.
- لا تُكتب كلمات مرور التوقيع في Git أو سجلات المشروع.

## 10) تاريخ الإصدارات المهمة
- v0.7: تحسينات المعاينة، حالة التحرير، التنقل السريع وشريط الأدوات.
- v0.8.0: تنظيف المعمارية، مصدر مباشر، تقليل الحجم، Release pipeline، Search & Replace.
- v0.8.1: `FlingEditText` حقيقي + إصلاح البحث/الكيبورد + قوائم قابلة للتمرير؛ اعتمد بعد اختبار الهاتف ودُمج عبر PR #2.
- v0.9.0: تنزيل ملفات Markdown العامة مباشرة من GitHub؛ نجح CI واختبار الجهاز، واعتمد المستخدم ثم دُمج عبر PR #4 إلى `main`.
- squash merge commit لـ v0.9.0: `87f6485dc2b98e339555154e6b8cd20666a2186c`.
- v0.10.0: تحسين task lists/إجابات الأسئلة وأداء الملفات الكبيرة؛ اجتاز CI واختبار الهاتف، وثُبّت كتحديث موقّع بنجاح، واعتمده المستخدم وأعطى موافقة صريحة على دمج PR #5.

## 11) v0.10.0 — Task answers + large documents

### سبب التغيير
- ظهر في الاستخدام الفعلي أن صيغة مثل `- [ x ] الرأسي` قد تظهر كنص عادي بدل مربع محدد.
- السبب أن `markdown-it` في التطبيق لا يضيف task-list plugin تلقائيًا، بينما المعالجة السابقة كانت تتعرف فقط على `[x]` و`[ ]` بالشكل الحرفي بعد أن تصبح السطر قائمة صحيحة.
- المطلوب كذلك أن تكون الإجابة الصحيحة واضحة بصريًا بلون مناسب، وأن تتوفر أدوات ذلك داخل محرر التطبيق، مع تحسين السلاسة في ملفات Markdown الكبيرة.

### الإصدار والاعتماد
- `versionName`: **0.10.0**.
- `versionCode`: **13**.
- PR: **#5 — MD Reader v0.10.0 — إجابات Markdown وأداء الملفات الكبيرة**.
- اختُبرت النسخة الموقعة فعليًا على الهاتف كتحديث فوق النسخة السابقة.
- أكد المستخدم أن التطبيق يعمل **بكل سلاسة** وأعطى موافقة صريحة على التوثيق والدمج في 2026-09-16.

### Task lists والإجابات الصحيحة
- `reader.html` يطبّع في **المعاينة فقط** الصيغ الشائعة إلى صيغة موحدة، ومنها:
  - `- [x]`
  - `-[x]`
  - `- [ x ]`
  - `* [X]`
  - `+ [ ]`
- لم نعد نعتمد على checkbox أصلي disabled في WebView؛ يرسم MD Reader مربعًا خاصًا به ويضع `✓` صريحة للحالة المحددة.
- السطر المحدد `- [x]` يظهر بخلفية خضراء خفيفة، نص أوضح، وحد جانبي أخضر متوافق مع RTL/LTR.
- السطر الذي يبدأ أصلًا بـ `✅` يحصل أيضًا على تمييز الإجابة الصحيحة في المعاينة بدون الحاجة إلى HTML داخل ملف Markdown.
- بقي `html:false` في `markdown-it`؛ لم نفتح HTML عشوائي داخل ملفات المستخدم لأن التمييز المطلوب أصبح مدعومًا بشكل أصلي وآمن داخل العارض.

### أدوات التحرير الجديدة
داخل **أدوات Markdown إضافية**:
- `✅ إجابة صحيحة`: يحول السطر الحالي أو الأسطر المحددة إلى `- [x] ...`.
- `☐ اختيار غير محدد`: يحولها إلى `- [ ] ...`.
- `☑ قائمة مهام`.
- `🧹 توحيد مربعات الاختيار`: يصلح الصيغ غير الموحدة داخل المستند كله إلى `- [x]` / `- [ ]`، ويحوّل سطر `✅ ...` إلى إجابة محددة عند طلب التوحيد.
- الجدول والفاصل الأفقي بقيا كما هما.
- زر task السريع في شريط التحرير أصبح يستخدم منطق السطر نفسه بدل إضافة prefix أعمى قد ينتج صياغة مكسورة.
- منطق Java لا يعتمد `String.stripLeading()` أو `String.isBlank()` حتى يبقى متوافقًا مع **minSdk 26**.

### تحسين الملفات الكبيرة
- Base64 الخاص بالمعاينة يُجهّز على executor منفصل (`previewIo`) بدل تنفيذه على UI thread.
- النقل إلى WebView يتم على دفعات `90,000` حرف Base64 بدل تمرير payload ضخم في استدعاء JavaScript واحد.
- render requests لها token + debounce لمنع إكمال معاينة قديمة بعد وصول طلب أحدث.
- عند المستندات من **180,000 حرف فأكثر** تُفعّل `content-visibility:auto` للكتل العليا حتى يؤجل Chromium رسم الأجزاء البعيدة عن الشاشة.
- Syntax highlighting يؤجل باستخدام `IntersectionObserver` حتى تقترب كتلة الكود من الشاشة.
- Mermaid في الملف الكبير يؤجل بالطريقة نفسها بدل رسم كل المخططات فورًا.
- في وضع التحرير، الملفات من **120,000 حرف فأكثر** لا تنسخ النص الكامل وتفحصه عند كل ضغطة؛ أعمال history/draft/search الثقيلة تُجمع بعد هدوء قصير (`220ms`).
- حد سجل Undo للملفات الكبيرة خُفض إلى **20 snapshot** بدل 80 لتقليل ضغط الذاكرة، مع بقاء الحد القديم للملفات الأصغر.
- بحث المحرر يأخذ snapshot واحدًا للنص بدل تحويل محتوى المحرر إلى String مرتين في العملية نفسها.
- تم اختيار threshold المعاينة بحيث يغطي بنك الأسئلة الفعلي الذي كان حجمه نحو **242KB** أثناء هذه المهمة.

### نتيجة الاختبار النهائي
- CI: ناجح.
- بناء APK/AAB والتحقق من ABI: ناجح.
- رفع Artifact بعد جعل المستودع عامًا: ناجح.
- تثبيت النسخة الموقعة بنفس مفتاح Release كتحديث فوق v0.9.0: ناجح.
- اختبار الهاتف: المستخدم أكد أن التطبيق يعمل بكل سلاسة.
- قرار الاعتماد: **معتمد للدمج في `main`**.

## 12) قاعدة العمل للإصدار التالي
قبل أي تعديل جديد:
1. قراءة هذا الملف أولًا.
2. إنشاء فرع جديد من `main` المستقر.
3. عدم تغيير package أو signing key.
4. أي إصدار لاحق يجب أن يستخدم `versionCode` أعلى من **14**.
5. الحفاظ على `FlingEditText` وسلوك GitHub import المعتمد ما لم يوجد بديل مثبت أفضل.
6. الحفاظ على task normalization/renderer المعتمد وعدم العودة للاعتماد على native disabled checkbox وحده.
7. إضافة/تحديث الاختبارات المناسبة.
8. بناء Release والتحقق من الحجم والتوقيع والـABI.
9. اختبار أي UX حساس للمس/الكيبورد/التنزيل على جهاز فعلي.
10. تحديث هذا الملف قبل اعتماد أي إصدار جديد.
11. لا دمج إلى `main` بدون موافقة المستخدم الصريحة.

## 13) v0.11.0 Candidate — Safe HTML Layer + Editor Tools
- سبب الإصدار: v0.10.0 أبقى `html:false` وحل task-list فقط؛ المستخدم طلب دعم HTML عامًا بشكل معماري وليس حلولًا مخصصة لكل وسم.
- الفرع: `feature/safe-html-layer-tools`.
- الإصدار المرشح: `versionName 0.11.0` / `versionCode 14`.
- `markdown-it` أصبح `html:true` لكن **لا يتم إدخال الناتج إلى DOM مباشرة**. يمر HTML كاملًا عبر DOMPurify محلي مضمّن داخل APK ثم عبر سياسة `mdreader-safe-html.js`.
- السياسة Allowlist وليست blocklist فقط: تسمح بعناصر تنسيق شائعة وآمنة مثل `mark`, `span`, `u`, `sub`, `sup`, `kbd`, `small`, `details/summary`, الجداول، القوائم، العناوين، الروابط والصور.
- العناصر الخطرة محظورة صراحة: `script`, `style`, `iframe`, `object`, `embed`, `form`, حقول الإدخال، `svg/math`, الوسائط و`canvas`. Event handlers لا يسمح بها DOMPurify.
- `style` لا يمر كما هو: تتم إعادة بنائه من خصائص محددة فقط (`color`, `background-color`, `text-align`, `font-weight`, `font-style`, `text-decoration`, `font-size`, `display`) مع تحقق للقيم ومنع `url()`, `expression()` و`javascript:`.
- `class` يقتصر على أصناف لغة كتل الكود (`language-*` / `lang-*`) للمحافظة على syntax highlighting دون فتح تنسيقات عشوائية.
- أضيف CSP داخل `reader.html` كطبقة دفاع إضافية: لا frames/objects/forms/connect، والسكربتات محلية فقط.
- DOMPurify يتم جلبه بإصدار ثابت `3.2.6` أثناء البناء ويعمل محليًا داخل التطبيق؛ لا تعتمد المعاينة على CDN وقت التشغيل.
- أدوات التحرير الجديدة داخل **HTML آمن**: تظليل، تسطير، لون نص، لون خلفية، محاذاة، RTL/LTR/Auto، نص علوي وسفلي، kbd، small، details، و`<br>`.
- ألوان مخصصة تقبل HEX فقط من الواجهة، وتخضع لفحص Java عند الإدراج ثم لفحص sanitizer مرة ثانية عند المعاينة.
- أضيف `SafeHtmlEditorTools.java` كطبقة تحويل Pure Java واختبار smoke مستقل بدل حشر منطق HTML داخل `MainActivity`.
- GitHub Actions Run #87 (`35278414146`) انتهى **Success**: فحوص المعمارية وJava/JavaScript وسياسة HTML الآمنة وملفات Markdown الكبيرة وبناء APK/AAB وفحص ABI ورفع Artifact.
- تم توقيع APK المرشح بنفس مفتاح Release الدائم، وتثبيته كتحديث فوق v0.10.0 على هاتف المستخدم بنجاح.
- اختبار الهاتف في 2026-09-18: المستخدم أكد أن النسخة **ممتازة جدًا حاليًا** وأن طبقة HTML والأدوات تعمل كما يريد، ثم أعطى موافقة صريحة على الدمج.
- قرار الاعتماد: **v0.11.0 معتمد للدمج إلى `main` عبر PR #6**.


## 14) v0.12.0 Candidate — Named Code Block Titles
- الفرع: `feature/code-block-titles`.
- الإصدار المرشح: `versionName 0.12.0` / `versionCode 15`.
- الهدف: دعم عنوان/اسم ملف اختياري داخل حاوية fenced code بدون كسر syntax highlighting أو زر النسخ أو RTL/LTR.
- الصيغة الأساسية المعتمدة: ```python title="app.py"`.
- الصيغة المختصرة المدعومة: ```python:app.py`.
- كتل الكود التقليدية بدون عنوان تبقى متوافقة كما هي.
- أضيف `mdreader-code-fence.js` كـ parser صغير مستقل بدل حشر parsing داخل `reader.html`، مع حدود وتنظيف للغة والعنوان.
- ينقل العارض العنوان مؤقتًا عبر attribute آمن `title` المسموح أصلًا في طبقة DOMPurify، ثم يحوله بعد التعقيم إلى header DOM محلي؛ لا يتم السماح بـ classes/styles عشوائية قادمة من Markdown.
- أضيف header مرئي متوافق مع الوضعين الفاتح/الداكن ومع اتجاه النص، ويظل زر النسخ داخل header عند وجود عنوان.
- أضيف `tools/code-fence-meta-smoke-test.js` لاختبار title المقتبس وغير المقتبس، shorthand، C++، التوافق مع fence القديم، وتنظيف المدخلات.
- تم تحديث CI ليفحص parser الجديد، يشغل smoke test، ويتأكد من تضمين asset داخل APK.
- أضيف داخل **أدوات Markdown وHTML** اختصار متناسق بصريًا باسم **`▣ حاوية بعنوان…`** بجانب أدوات HTML والجدول والفاصل.
- الاختصار يفتح حوارًا باسم الحاوية + لغة تلوين اختيارية؛ الاسم يقبل العربية والإنجليزية، واللغة مثل `python` / `json` ويمكن تركها فارغة.
- إذا كان هناك نص محدد يلفه داخل الحاوية؛ وإذا لم يوجد تحديد يدرج `المحتوى` ويحدده مباشرة للكتابة.
- منطق الإدراج أضيف داخل `MarkdownTransforms.titledFencedCode` للحفاظ على تحويلات المحرر Pure Java وعدم حشرها في `MainActivity`.
- يختار طول fence ديناميكيًا إذا كان المحتوى نفسه يحتوي backticks، ويضيف فراغات كتلية مناسبة عند الإدراج داخل سطر.
- parser أصبح يدعم escaped quotes داخل العنوان، لذلك أسماء مثل `شرح "المثال الأول"` تعرض بشكل صحيح.
- توسعت اختبارات `TransformSmokeTest` و`code-fence-meta-smoke-test.js` لتغطي الاسم العربي، اللغة الاختيارية، الاقتباسات، النص المحدد وnested backticks.
- GitHub Actions Run `36168040146` انتهى **Success** على الكود الذي يتضمن الاختصار: نجحت Assertions، Java smoke tests، JavaScript/parser tests، safe HTML compatibility، بناء ARM64 APK، بناء AAB، فحص ABI/package ورفع Artifact.
- تم تجهيز APK موقّع للاختبار من Artifact الخاص بـ Run `36168109617` باستخدام نفس مفتاح Release الدائم؛ SHA-256: `d25d7a312d3449bb5c9853e956e88cebda6a806aa2807be70d960f63db9d9318`.
- شهادة التوقيع SHA-256: `3d7f963db1c9b5211d3a7f0c227d208108474b390802afa6891133a035bf835a`، مطابقة للإصدارات السابقة، وv2/v3 مفعّلان.
- اختبار الجهاز في 2026-09-25: المستخدم أكد أن نسخة v0.12.0 الموقعة مع اختصار **▣ حاوية بعنوان…** تعمل بشكل ممتاز على الهاتف.
- الحالة الحالية: التنفيذ والتوثيق وCI والتوقيع واختبار الجهاز مكتملة بنجاح. لا يدمج إلى `main` قبل موافقة المستخدم الصريحة على الدمج.
