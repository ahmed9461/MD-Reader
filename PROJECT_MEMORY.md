# MD Reader — Project Memory & Source of Truth

> اقرأ هذا الملف والخطة النشطة قبل التعديل. السجل التفصيلي السابق محفوظ **حرفيًا دون حذف** في `docs/history/PROJECT_MEMORY-v0.12.0.md`، بما فيه الإصدارات والفحوص وقرارات HTML والأداء والتوقيع. هذه الصفحة تفصل الحالة النشطة عن التاريخ لتجنب اعتماد مرشح تجريبي بالخطأ.

## الحالة الحالية — 2026-09-26

- المستودع: `ahmed9461/MD-Reader`.
- المستقر على `main`: **v0.12.0 / versionCode 15** عند `683c5c93ffe4674169039391e985bb9de2b24299`، اعتمده المستخدم على هاتفه ثم وافق على دمج PR #7.
- العمل النشط: **v0.13.0 / versionCode 16** على `feature/ui-ux-refresh`.
- الخطة: `plans/0013-ui-ux-refresh.md`؛ لا تبدأ خطة مكررة.
- **v0.13.0 غير معتمد وغير مدمج؛ اختبار الهاتف وموافقة الدمج الصريحة ما زالا مطلوبين.**
- الحزمة الدائمة: `app.mdreader.mobile`، Android minSdk 26 / compile+target 36.
- Native Java + Android WebView؛ واجهة عربية RTL ومحتوى عربي/إنجليزي ثنائي الاتجاه.
- نتائج الإصدار المرشح وبيانات التسليم تحفظ في `docs/UI_REDESIGN_VALIDATION.md` عند اكتمال الفحص؛ راجع أيضًا `PROGRESS_LOG.md` وCI الخاص بالمرشح الفعلي، لا حالة إصدار قديم.

## المطلوب والقرار المعماري

المستخدم راضٍ عن الوظائف الحالية ويريد تجديدًا شاملًا للواجهات والتبويبات والأزرار والتنقل والأحجام، وليس إعادة بناء وظائف التطبيق. المثال الأساسي: البحث والاستبدال كان يغطي المستند ويمنع رؤية نتائج السابق/التالي. أيقونة التطبيق هي صورة `.md` التي قدمها المستخدم، بنقطة برتقالية وحروف بيضاء وخلفية داكنة.

- `UiPalette`: ألوان موحدة للوضعين الفاتح والداكن، برتقالي هادئ، ألوان مستقلة للنص فوق اللون الأساسي.
- `ReaderUi` و`UiIcon`: مسافات وحالات وتلميحات وأيقونات موحدة؛ أهداف لمس لا تقل عن 48dp للأزرار الرئيسية.
- `FindReplaceBar`: شريط **داخل التخطيط** لا نافذة فوق النص؛ البحث بسطرين وخيارات استبدال قابلة للطي، وعدّاد عربي «1 من 3». السابق/التالي يخفيان الكيبورد دون سرقة تركيز حقل البحث ويُظهران النتيجة في المساحة المتبقية.
- `SearchMatchState`: مسح literal غير متداخل مع لف النتائج؛ إصلاح السابق عند أول نتيجة؛ لا حد اصطناعي لعدد النتائج.
- `ResponsiveSheet`: عنوان وإغلاق ثابتان، جسم واحد قابل للتمرير، عرض وارتفاع محدودان، Insets للنظام والكيبورد. تجنب ScrollView متداخل بوزن 1 وارتفاع صفر.
- المكتبة: الرئيسية، المستندات، المفضلة، الإعدادات؛ بطاقات ملفات وأزرار نجمة/المزيد منفصلة. مساحة المستند: شريط علوي أخف، قراءة/تحرير، فهرس وعلامات مرجعية.
- في مساحة أفقية قصيرة تُطوى الخيارات الثانوية للبحث مؤقتًا مع إبقاء البحث والسابق/التالي والإغلاق والنص متاحين.
- `SpeechSettingsDialog` يستخدم نفس مكونات الواجهة. اختيار مزود/محرك الترجمة يحدّث النص والخلفية معًا لضمان التباين.
- صورة المستخدم محفوظة في `drawable-nodpi/ic_launcher_art.webp` مع هوامش adaptive icon؛ ليست شعارًا بديلًا مولدًا.
- CSS المعاينة يتبع الهوية الجديدة، وأزرار نسخ الكود لا تغطي أول سطر. JS وCSP وتعقيم HTML بقيت قائمة.

## وظائف لا يجوز فقدها

فتح/حفظ/حفظ باسم عبر SAF، المشاركة والطباعة/PDF، المفضلة والسجل والمسودات واستعادة موضع القراءة والتحرير، Markdown/HTML tools، Undo/Redo، البحث والاستبدال Unicode ومتعدد الأسطر، الفهرس والتنقل والعلامات، Mermaid والجداول وsyntax highlighting، الترجمة المحلية وAI، القراءة المحلية وOpenAI TTS.

- `FlingEditText` + OverScroller/VelocityTracker؛ تمرير حركي حقيقي يتوقف باللمس، وسرعة محفوظة 10–500%.
- الملفات الكبيرة: debounce للأعمال الثقيلة، preview worker/chunks، lazy enhancement، history limit 20 للملفات الكبيرة؛ لا تعطلها في تجميل الواجهة.
- task answers: تطبيع صيغ `- [x]`, `-[x]`, `- [ x ]`, `* [X]`, `+ [ ]` في المعاينة فقط، وعلامة ✓ وتظليل أخضر واضح؛ لا تعتمد على checkbox disabled الأصلي.
- HTML الآمن: DOMPurify + allowlists + style sanitation + CSP؛ لا تفتح script/iframe/event attributes أو روابط خطرة.
- حاويات الكود: `python title="app.py"` و`python:app.py`، عناوين عربية/إنجليزية، زر النسخ، واختصار **▣ حاوية بعنوان…** داخل أدوات Markdown وHTML؛ fence ديناميكي للمحتوى الذي يحتوي backticks.
- GitHub import: روابط Markdown العامة عبر HTTPS فقط، redirects مقيدة بـ GitHub/raw، خارج UI thread، دون توكنات أو تسجيل دخول. تنزيل وفتح وتنزيل وحفظ كلاهما باقيان.
- لا إعلانات أو تحليلات أو تتبع، ولا حذف ML Kit أو الصوت أو Mermaid لتقليل الحجم دون قرار صريح.

## قواعد التنفيذ والتسليم

1. main يبقى مستقرًا. العمل على فرع، ثم CI وAPK موقّع واختبار هاتف وموافقة صريحة قبل الدمج.
2. المصدر Java مباشر داخل `app/src/main`؛ لا reconstruction أو patch chain في build النهائي. ملفات نقل المصدر المؤقتة ليست معمارية وتُزال قبل التسليم.
3. Release يستخدم R8 وresource shrinking؛ APK مباشر ARM64، وAAB متعدد المعماريات يبنى منفصلًا. اختبارات الأجهزة لا تُضمّن في APK release.
4. شغّل اختبارات Java السابقة، SearchMatchStateSmokeTest، اختبارات JS وعقود UI، ثم بناء APK/AAB وفحص ABI ومخرجات الرفع الفعلية.
5. `.github/workflows/ui-regression.yml` يشغّل محاكي API 35 ويلتقط صورًا فعلية. لا تساوِ المحاكي باختبار Samsung/هاتف المستخدم. الفحص لا يجري اتصال AI مدفوعًا ولا يستخدم مفاتيح المستخدم.
6. محاكي بارد قد يحتاج أكثر من ثانية لفتح IME؛ انتظر ظهور Insets فعليًا ضمن مهلة محدودة، ولا تحذف تحقق ظهور الكيبورد لتجاوز الاختبار.
7. لا تحذف artifacts لإصدارات أخرى من أجل حصة التخزين. لا تغير package أو signature. كل إصدار تسليم جديد يحتاج versionCode مناسبًا.
8. لا تُسجل كلمات مرور أو مفاتيح، ولا تُدخلها Git أو المستندات أو حزمة المصدر.

## التوقيع الدائم

PKCS12 خارج Git، alias `mdreader`. شهادة SHA-256:
`3d7f963db1c9b5211d3a7f0c227d208108474b390802afa6891133a035bf835a`.
تحقق من الشهادة وAPK Signature Scheme v2/v3 قبل التسليم. لا تنشئ مفتاحًا بديلًا. تاريخ وصلاحية المفتاح وبيانات الإصدارات القديمة في السجل المؤرشف؛ لا يحتوي المستودع على كلمات المرور.

## نقطة الاستكمال

أكمل فحص مرشح v0.13.0، راجع صور البحث مع الكيبورد والسابق/التالي واللوحات والدوران، ثم وثّق run/commit والشهادة وSHA-256 للنسخة الموقعة. بعد التسليم لا تعتبر النسخة معتمدة حتى يختبرها المستخدم ويوافق صراحةً على الدمج.
