# MD Reader — Project Memory & Source of Truth

> اقرأ هذا الملف والخطة النشطة وتقرير التحقق قبل التعديل. السجل السابق محفوظ **حرفيًا دون حذف** في `docs/history/PROJECT_MEMORY-v0.12.0.md`، بما فيه الإصدارات والفحوص وقرارات HTML والأداء والتوقيع. لا تعتمد مرشحًا تجريبيًا بالخطأ.

## الحالة الحالية — 2026-09-26

- المستودع: `ahmed9461/MD-Reader`.
- المستقر على `main`: **v0.12.0 / versionCode 15** عند `683c5c93ffe4674169039391e985bb9de2b24299`، اعتمده المستخدم على هاتفه ثم وافق على دمج PR #7.
- المرشح: **v0.13.0 / versionCode 16** على `feature/ui-ux-refresh`، **Draft PR #9**.
- الخطة: `plans/0013-ui-ux-refresh.md`؛ التنفيذ والفحص الآلي والتوقيع مكتملة، لا تبدأ خطة مكررة.
- المصدر الذي بُني واختُبر ووُقّع للتسليم: `a5974fb6eae24d9adb6643f5bc9cce361118d6dd`.
- Release run `36269131631` وUI run `36269131647`: ناجحان. 277 تحققًا لمحرك النتائج، و32 تحققًا على المحاكي، و13 صورة فعلية راجعتُها بصريًا.
- APK جاهز لتجربة الهاتف، بالتوقيع الدائم نفسه. التفاصيل والقيود وSHA-256 في `docs/UI_REDESIGN_VALIDATION.md`.
- **v0.13.0 غير معتمد وغير مدمج؛ تجربة الهاتف وموافقة الدمج الصريحة ما زالتا مطلوبتين.**
- الحزمة: `app.mdreader.mobile`، minSdk 26 / compile+target 36، Native Java + Android WebView، واجهة عربية RTL ومحتوى عربي/إنجليزي.
- التحديثات التوثيقية بعد المصدر المذكور لا تغير التطبيق الموقّع؛ لا تخلط artifacts القديمة مع مرشح التسليم.

## المطلوب والقرار المعماري

المستخدم راضٍ عن الوظائف الحالية ويريد تجديد الواجهات والتبويبات والأزرار والتنقل والأحجام. المثال الأساسي: البحث والاستبدال كان يغطي المستند ويمنع رؤية السابق/التالي. الأيقونة هي صورة `.md` المقدمة من المستخدم: نقطة برتقالية، حروف بيضاء، خلفية داكنة.

- `UiPalette`: ألوان موحدة للوضعين الفاتح والداكن، برتقالي هادئ، لون مستقل للنص فوق اللون الأساسي.
- `ReaderUi` و`UiIcon`: مسافات وحالات وتلميحات وأيقونات موحدة، وأهداف لمس رئيسية لا تقل عن 48dp.
- `FindReplaceBar`: شريط **داخل التخطيط**، بحث متعدد الأسطر وخيارات استبدال قابلة للطي، وعدّاد «1 من 3». السابق/التالي يخفيان الكيبورد دون نقل التركيز إلى المحرر، مع تمييز النتيجة وإظهارها.
- `SearchMatchState`: مسح literal غير متداخل مع لف النتائج؛ إصلاح السابق عند أول نتيجة؛ لا حد اصطناعي لعدد النتائج.
- `ResponsiveSheet`: عنوان وإغلاق ثابتان، جسم واحد قابل للتمرير، عرض/ارتفاع محدودان وInsets للنظام والكيبورد. تجنب ScrollView متداخل بوزن 1 وارتفاع صفر.
- المكتبة: الرئيسية، المستندات، المفضلة، الإعدادات؛ بطاقات ملفات وأزرار نجمة/المزيد مستقلة. المستند: شريط علوي أخف، قراءة/تحرير، فهرس وعلامات.
- عند ضيق الارتفاع جدًا مع عرض واسع، البحث بجانب المستند بدل فوقه؛ حقول الاستبدال تبقى متاحة عند توسيعها. يعود التخطيط العمودي عند توفر المساحة. تُخفى العناصر الثانوية أيضًا عند الكتابة العادية في مساحة كيبورد قصيرة.
- `SpeechSettingsDialog` يستخدم المكونات نفسها. أزرار إجراءات الصوت بحد أدنى 48dp. اختيار محرك/مزود الترجمة يحدّث لون النص والخلفية معًا.
- الصورة الأصلية في `drawable-nodpi/ic_launcher_art.webp` بهوامش adaptive icon؛ ليست شعارًا بديلًا مولدًا.
- CSS المعاينة يتبع الهوية الجديدة وأزرار نسخ الكود لا تغطي أول سطر. JS وCSP وتعقيم HTML بقيت قائمة.
- لا إطار عمل أو اعتماد واجهة إنتاجي جديد.

## وظائف لا يجوز فقدها

فتح/حفظ/حفظ باسم عبر SAF، المشاركة والطباعة/PDF، المفضلة والسجل والمسودات واستعادة موضع القراءة والتحرير، Markdown/HTML tools، Undo/Redo، البحث Unicode ومتعدد الأسطر، الفهرس والتنقل والعلامات، Mermaid والجداول وsyntax highlighting، الترجمة المحلية وAI، القراءة المحلية وOpenAI TTS.

- `FlingEditText` + OverScroller/VelocityTracker؛ تمرير حركي يتوقف باللمس وسرعة محفوظة 10–500%.
- الملفات الكبيرة: debounce للأعمال الثقيلة، preview worker/chunks، lazy enhancement، history limit 20؛ لا تعطلها لتجميل الواجهة.
- task answers: تطبيع `- [x]`, `-[x]`, `- [ x ]`, `* [X]`, `+ [ ]` في المعاينة فقط، وعلامة ✓ وتظليل أخضر؛ لا تعتمد على checkbox disabled الأصلي.
- HTML الآمن: DOMPurify + allowlists + style sanitation + CSP؛ لا تسمح بـ script/iframe/event attributes أو روابط خطرة.
- حاويات الكود: `python title="app.py"` و`python:app.py`، عناوين عربية/إنجليزية، نسخ، واختصار **▣ حاوية بعنوان…**؛ fence ديناميكي للمحتوى الذي يحتوي backticks.
- GitHub import: Markdown عام عبر HTTPS، redirects مقيدة بـ GitHub/raw، خارج UI thread، بلا توكنات/تسجيل دخول؛ تنزيل وفتح وتنزيل وحفظ.
- لا إعلانات أو تحليلات أو تتبع، ولا حذف ML Kit أو الصوت أو Mermaid لتقليل الحجم دون قرار صريح.

## قواعد التنفيذ والفحص

1. main يبقى مستقرًا. فرع تطوير، CI، APK موقّع، تجربة هاتف، وموافقة صريحة قبل الدمج.
2. Java مباشر في `app/src/main`؛ لا reconstruction أو patch chain في build النهائي. ملفات النقل المؤقتة أزيلت.
3. R8 وresource shrinking؛ APK مباشر ARM64، وAAB متعدد المعماريات منفصل. اختبارات الأجهزة لا تدخل APK release.
4. شغّل اختبارات Java وSearchMatchState وJS وعقود UI، ثم بناء APK/AAB وفحص ABI ومخرجات الرفع الحقيقية.
5. المحاكي API 35 يشغّل debug من المصدر نفسه؛ بناء/توقيع release فحص منفصل. لا تساوِ ذلك بتجربة النسخة الموقعة على Samsung أو هاتف المستخدم.
6. IME البارد يحتاج انتظار Insets فعلية بمهلة محدودة؛ لا تحذف تحقق ظهوره لتجاوز الاختبار. مشكلة مساحة النص الأفقية اكتُشفت فعليًا وأُصلحت، وأضيف اختبار للتحرير العادي أيضًا.
7. لا تحذف artifacts أخرى لمعالجة حصة التخزين. لا تغير الحزمة أو المفتاح. كل إصدار تسليم جديد يحتاج versionCode مناسبًا.
8. لا تسجل مفاتيح أو كلمات مرور ولا تدخلها Git أو حزم المصدر. اختبار UI لم يستخدم مفاتيح المستخدم أو اتصالات AI مدفوعة.
9. مراجعة المحاكي لا تغطي كل إصدارات Android أو كل أحجام الخط/لوحات المفاتيح أو جميع موفري SAF. الاستخدام الحقيقي والخدمات الخارجية واختبار أداء كبير جديد لم تُختبر بالكامل؛ راجع تقرير التحقق ولا تدّعِ خلاف ذلك.

## التوقيع الدائم

PKCS12 خارج Git، alias `mdreader`. شهادة SHA-256:
`3d7f963db1c9b5211d3a7f0c227d208108474b390802afa6891133a035bf835a`.
تم التحقق من v2/v3 وتطابق الشهادة وهوية الحزمة في APK المرشح. لا تنشئ مفتاحًا بديلًا. لا كلمات مرور في المستودع؛ تاريخ الإصدارات في السجل المؤرشف.

## نقطة الاستكمال

انتظار ملاحظات المستخدم على النسخة الموقعة v0.13.0. عند ورود مشكلة أعد إنتاجها ثم أصلحها على الفرع نفسه وحدّث الخطة والتقرير. لا تكرر تنفيذ إعادة التصميم أو تنسخ الذاكرة في الرد. قبل الدمج يلزم اختبار الهاتف وموافقة صريحة وفحص الحالة الحالية للمستودع وCI؛ لا تعتبر نجاح المحاكي موافقة من المستخدم.
