# MD Reader — Project Memory & Source of Truth

> هذا الملف هو المرجع الأساسي للمشروع. يجب قراءته قبل أي تعديل مهم، وتحديثه بعد كل إصدار أو قرار معماري مؤثر.

## 1) الحالة الحالية
- الاسم: **MD Reader**.
- الحزمة الثابتة: `app.mdreader.mobile`.
- الإصدار المستقر المعتمد: **v0.9.0**.
- `versionCode`: **12** للإصدار المستقر على `main`.
- يوجد مرشح **v0.10.0 / versionCode 13** قيد الاختبار على الفرع `feature/task-answer-highlights-performance`، ولم يُعتمد أو يُدمج إلى `main` بعد.
- Android: minSdk 26، compile/targetSdk 36.
- الواجهة: عربية RTL مع دعم الإنجليزية LTR.
- الحالة المستقرة: **Stable / v0.9.0 معتمد بعد اختبار فعلي على هاتف المستخدم**.
- تم اعتماد v0.9.0 ودمج PR #4 إلى `main` بطريقة squash بعد موافقة المستخدم الصريحة.
- PR #3 كان Draft لنفس الميزة وأُغلق دون دمج، ثم استُبدل بـPR #4 الجاهز للدمج.
- فلسفة المشروع: تطبيق خفيف وعملي، محلي قدر الإمكان، بلا إعلانات أو تحليلات أو تتبع، ولا تُضحّى الميزات الشغالة لمجرد تبسيط التنفيذ.

## 2) قواعد هندسية ثابتة
1. لا حلول ترقيعية أو string-patching كمعمارية دائمة.
2. `MainActivity.java` مصدر Java مباشر داخل `app/src/main/...`، ولا توجد patch/reconstruction chain في البناء النهائي.
3. كل إصدار تسليم يمر عبر CI واختبارات smoke وفحص JavaScript وبناء Release والتحقق من APK/AAB.
4. التوزيع يكون Release بمفتاح توقيع ثابت، وليس debug key.
5. الـkeystore وكلمات المرور لا تدخل Git أو `build.gradle`؛ تحفظ خارج المستودع وتُمرر عبر environment/secrets.
6. لا يتغير `applicationId` ولا مفتاح التوقيع بين الإصدارات، ويجب رفع `versionCode` لكل إصدار جديد.
7. أي تغيير في التحرير أو التمرير أو الكيبورد أو القوائم يجب اختباره على جهاز فعلي قبل الاعتماد.
8. لا يُحذف ML Kit أو Mermaid أو الصوت أو أي وظيفة قائمة بهدف خفض الحجم دون قرار صريح موثق.
9. العمل على أي إصدار جديد يبدأ من `main` المستقر وعلى فرع منفصل، ولا يُدمج قبل موافقة المستخدم الصريحة.
10. لا نعتمد على سلوك checkbox الأصلي في Android WebView إذا كان يسبب عرضًا غير متناسق؛ المعاينة يجب أن تكون حتمية بين الإصدارات المدعومة.

## 3) الوظائف الأساسية المعتمدة
- فتح/تحرير/حفظ Markdown + وضع معاينة.
- RTL/LTR حسب المحتوى.
- أدوات Markdown: عناوين، غامق، مائل، strike، code/fenced code، اقتباس، قوائم، task list، روابط، صور، جداول وفاصل أفقي.
- Undo/Redo.
- البحث داخل المستند.
- Search & Replace literal Unicode غير محدود بعدد نتائج اصطناعي.
- فهرس العناوين والتنقل السريع.
- الملفات الأخيرة والمفضلة.
- استعادة المسودات.
- حفظ موضع القراءة وحالة التحرير `selectionStart/selectionEnd/scrollY` لكل مستند.
- علامات مرجعية.
- Syntax highlighting وMermaid.
- الترجمة المحلية عبر ML Kit وخيارات AI الموجودة سابقًا.
- القراءة بالصوت المحلي وخيار OpenAI TTS.
- Enter واحد يظهر كسطر جديد في المعاينة (`breaks:true`).
- شريط أدوات التحرير يبقى قابلًا للوصول مع لوحة المفاتيح.

## 4) تجربة التحرير والتمرير
- التنفيذ القديم `AdjustableScroller extends Scroller` فشل عمليًا في إعطاء fling محسوس وأزيل نهائيًا.
- الحل المعتمد منذ v0.8.1 هو `FlingEditText.java` باستخدام `OverScroller + VelocityTracker`.
- swipe سريع ثم رفع الإصبع = استمرار تمرير حركي حقيقي.
- لمس الشاشة أثناء الحركة = إيقاف فوري للـfling.
- سرعة الـfling قابلة للتعديل من **10% إلى 500%** وتحفظ في SharedPreferences.
- تم اختبار هذا السلوك فعليًا على الهاتف واعتماده.

## 5) القوائم والكيبورد
- القوائم الطويلة تستخدم `ScrollView` بحيث تبقى كل الأزرار قابلة للوصول.
- نافذة البحث والاستبدال بحجم محتواها وليست panel ثابتة الارتفاع.
- حقلا البحث والاستبدال يدعمان نصًا متعدد الأسطر.
- Dialog يستخدم `SOFT_INPUT_ADJUST_RESIZE` لتقليل تغطية لوحة المفاتيح للحقول.

## 6) GitHub Markdown Import — v0.9.0
- الملف المسؤول: `GitHubMarkdownSource.java`.
- يوجد زر **تنزيل ملف من GitHub** في الصفحة الرئيسية وخيار مقابل في قائمة الثلاث نقاط.
- يقبل روابط GitHub العامة بصيغة `https://github.com/<owner>/<repo>/blob/<ref>/<path>.md`.
- يقبل أيضًا روابط `raw.githubusercontent.com` المباشرة.
- خيار **تنزيل وفتح** يحمّل الملف ويفتحه مباشرة داخل MD Reader.
- خيار **تنزيل وحفظ في الجهاز** يحمّل الملف ثم يفتح Android document picker لحفظ نسخة محلية.
- التنزيل يتم خارج UI thread حتى لا تتجمد الواجهة.
- HTTPS فقط.
- لا تُرسل Tokens أو بيانات دخول.
- إعادة التوجيه مقيدة إلى GitHub و`raw.githubusercontent.com`.
- المستودعات الخاصة غير مدعومة في v0.9.0؛ لا تُضاف Tokens يدويًا داخل التطبيق دون تصميم مصادقة آمن.
- الرابط المرجعي الذي اختُبر أثناء التطوير: `https://github.com/ahmed9461/GitDock/blob/main/CHANGELOG.md`.
- تم اختبار الميزة فعليًا على هاتف المستخدم، وأكد أنها تعمل بنجاح قبل الاعتماد.

## 7) الحجم والبناء
- البناء Release يستخدم R8 (`minifyEnabled true`) و`shrinkResources true`.
- APK المباشر مخصص لـ `arm64-v8a` لتجنب حمل مكتبات معماريات غير مطلوبة.
- AAB يبنى بشكل منفصل للنشر متعدد المعماريات.
- ML Kit Translation بقي موجودًا.

### ناتج v0.9.0 المختبر على الجهاز
- `MD-Reader-v0.9.0-arm64-release.apk`
- الحجم: **18,232,410 bytes**.
- SHA-256: `6fd4edd298db3d1a272145b8a12fe63a1ccb2227366bedbb1923141cf7b4f681`.
- موقّع بنفس مفتاح Release المستمر، ولذلك يثبت كتحديث فوق الإصدارات السابقة الموقعة بنفس المفتاح.

## 8) CI والتحقق لـ v0.9.0
- GitHub Actions Run #67: id `34655147435`.
- المحاولة الأولى نجحت في الاختبارات والبناء والتحقق من APK/AAB، وفشلت فقط عند رفع Artifact بسبب Actions Storage quota.
- بعد جعل المستودع عامًا مؤقتًا، أُعيد تشغيل الـrun.
- المحاولة الثانية انتهت **Success بالكامل**، بما في ذلك:
  - direct-source architecture assertions.
  - Markdown/Translation/Speech/Search smoke tests.
  - `GitHubMarkdownSourceSmokeTest`.
  - JavaScript syntax check.
  - ARM64 optimized Release APK build.
  - App Bundle build منفصل.
  - ABI/output verification.
  - رفع Artifact بنجاح.
  - حذف Artifacts الأقدم بنجاح.
- Artifact الناجح: `MD-Reader-v0.9.0-release-unsigned`.

## 9) التوقيع الدائم
- مفتاح Release ثابت موجود خارج Git وتم تسليمه لصاحب المشروع.
- النوع: PKCS12 داخل `md-reader-release.jks`.
- alias: `mdreader`.
- شهادة SHA-256:
  `3D:7F:96:3D:B1:C9:B5:21:1D:3A:7F:0C:22:7D:20:81:08:47:4B:39:08:02:AF:A6:89:11:33:A0:35:BF:83:5A`
- الصلاحية حتى 2054-01-26.
- يجب استخدام **نفس المفتاح** لكل الإصدارات القادمة.
- لا تُكتب كلمات مرور التوقيع في Git أو سجلات المشروع.

## 10) تاريخ الإصدارات المهمة
- v0.7: تحسينات المعاينة، حالة التحرير، التنقل السريع وشريط الأدوات.
- v0.8.0: تنظيف المعمارية، مصدر مباشر، تقليل الحجم، Release pipeline، Search & Replace.
- v0.8.1: `FlingEditText` حقيقي + إصلاح البحث/الكيبورد + قوائم قابلة للتمرير؛ اعتمد بعد اختبار الهاتف ودُمج عبر PR #2.
- v0.9.0: تنزيل ملفات Markdown العامة مباشرة من GitHub؛ نجح CI واختبار الجهاز، واعتمد المستخدم ثم دُمج عبر PR #4 إلى `main`.
- squash merge commit لـ v0.9.0: `87f6485dc2b98e339555154e6b8cd20666a2186c`.
- v0.10.0: **مرشح قيد الاختبار** لتحسين task lists/إجابات الأسئلة وأداء الملفات الكبيرة؛ لا يزال على فرع feature ولم يُعتمد بعد.

## 11) v0.10.0 Candidate — Task answers + large documents

### سبب التغيير
- ظهر في الاستخدام الفعلي أن صيغة مثل `- [ x ] الرأسي` قد تظهر كنص عادي بدل مربع محدد.
- السبب أن `markdown-it` في التطبيق لا يضيف task-list plugin تلقائيًا، بينما المعالجة السابقة كانت تتعرف فقط على `[x]` و`[ ]` بالشكل الحرفي بعد أن تصبح السطر قائمة صحيحة.
- المطلوب كذلك أن تكون الإجابة الصحيحة واضحة بصريًا بلون مناسب، وأن تتوفر أدوات ذلك داخل محرر التطبيق، مع تحسين السلاسة في ملفات Markdown الكبيرة.

### الفرع والإصدار
- الفرع: `feature/task-answer-highlights-performance`.
- `versionName`: **0.10.0**.
- `versionCode`: **13**.
- Draft PR: **#5 — MD Reader v0.10.0 — إجابات Markdown وأداء الملفات الكبيرة**.
- `main` بقي على v0.9.0؛ لا دمج قبل اختبار الهاتف وموافقة المستخدم.

### Task lists والإجابات الصحيحة
- `reader.html` يطبّع في **المعاينة فقط** الصيغ الشائعة إلى صيغة موحدة، ومنها:
  - `- [x]`
  - `-[x]`
  - `- [ x ]`
  - `* [X]`
  - `+ [ ]`
- لم نعد نعتمد على checkbox أصلي disabled في WebView؛ يرسم MD Reader مربعًا خاصًا به ويضع `✓` صريحة للحالة المحددة، لتكون النتيجة متناسقة بين إصدارات Android/WebView.
- السطر المحدد `- [x]` يظهر بخلفية خضراء خفيفة، نص أوضح، وحد جانبي أخضر متوافق مع RTL/LTR.
- السطر الذي يبدأ أصلًا بـ `✅` يحصل أيضًا على تمييز الإجابة الصحيحة في المعاينة بدون الحاجة إلى HTML داخل ملف Markdown.
- بقي `html:false` في `markdown-it`؛ لم نفتح HTML عشوائي داخل ملفات المستخدم لأن التمييز المطلوب أصبح مدعومًا بشكل أصلي وآمن داخل العارض.

### أدوات التحرير الجديدة
داخل **أدوات Markdown إضافية** أضيفت:
- `✅ إجابة صحيحة`: يحول السطر الحالي أو الأسطر المحددة إلى `- [x] ...`.
- `☐ اختيار غير محدد`: يحولها إلى `- [ ] ...`.
- `☑ قائمة مهام`.
- `🧹 توحيد مربعات الاختيار`: يصلح الصيغ غير الموحدة داخل المستند كله إلى `- [x]` / `- [ ]`، ويحوّل سطر `✅ ...` إلى إجابة محددة عند طلب التوحيد.
- الجدول والفاصل الأفقي بقيا كما هما.
- زر task السريع في شريط التحرير أصبح يستخدم منطق السطر نفسه بدل إضافة prefix أعمى قد ينتج صياغة مكسورة.
- منطق Java الجديد لا يعتمد `String.stripLeading()` أو `String.isBlank()` حتى يبقى متوافقًا مع **minSdk 26**.

### تحسين الملفات الكبيرة
- Base64 الخاص بالمعاينة يُجهّز على executor منفصل (`previewIo`) بدل تنفيذه على UI thread.
- النقل إلى WebView يتم على دفعات `90,000` حرف Base64 بدل تمرير payload ضخم في استدعاء JavaScript واحد.
- render requests لها token + debounce لمنع إكمال معاينة قديمة بعد وصول طلب أحدث.
- عند المستندات من **180,000 حرف فأكثر** تُفعّل `content-visibility:auto` للكتل العليا حتى يؤجل Chromium رسم الأجزاء البعيدة عن الشاشة.
- Syntax highlighting يؤجل باستخدام `IntersectionObserver` حتى تقترب كتلة الكود من الشاشة.
- Mermaid في الملف الكبير يؤجل بالطريقة نفسها بدل رسم كل المخططات فورًا.
- في وضع التحرير، الملفات من **120,000 حرف فأكثر** لا تنسخ النص الكامل وتفحصه عند كل ضغطة؛ أعمال history/draft/search الثقيلة تُجمع بعد هدوء قصير (`220ms`).
- حد سجل Undo للملفات الكبيرة خُفض إلى **20 snapshot** بدل 80 لتقليل ضغط الذاكرة، مع بقاء الحد القديم للملفات الأصغر.
- بحث المحرر أصبح يأخذ snapshot واحدًا للنص بدل تحويل محتوى المحرر إلى String مرتين في العملية نفسها.
- تم اختيار threshold المعاينة بحيث يغطي أيضًا بنك الأسئلة الفعلي الذي كان حجمه نحو **242KB** أثناء هذه المهمة.

### CI والبناء أثناء التطوير
- تم تحديث pipeline إلى v0.10.0 وإضافة assertions لـ task rendering، chunked preview، التوافق مع minSdk، وفحص JavaScript.
- ظهر عطل بنية تحتية في `android-actions/setup-android@v4` لأنه حاول تثبيت package قديم باسم `tools`; تم إزالة الاعتماد على تلك الخطوة واستخدام Android SDK الموجود على GitHub Hosted Runner مع تثبيت `platform-tools`, `android-36`, و`build-tools 36.0.0` مباشرة.
- تم حذف كل workflows/scripts المؤقتة التي استُخدمت لنقل التعديلات إلى المصدر المباشر؛ النتيجة النهائية بقيت direct-source كما تشترط قواعد المشروع.
- GitHub Actions **Run #80** — id `35131485730` — على commit `b6bfd97bace8fbeea1e5da4f1fd269bbb14301ad` انتهى **Success** بالكامل في التحقق من الكود والبناء.
- نجح في هذا الـrun:
  - direct-source architecture + v0.10 assertions.
  - Java smoke tests: Transform / Translation / Speech / SearchReplace / GitHubMarkdownSource.
  - JavaScript syntax check.
  - task syntax + large-document compatibility assertions.
  - ARM64 optimized Release APK build.
  - App Bundle Release build.
  - ABI/output verification.
- ناتج البناء المتحقق منه:
  - `MD-Reader-v0.10.0-arm64-release-unsigned.apk` — **18,200,803 bytes** — SHA-256 `d43efea53a183e151e4ccc0c3e7538ac97c966e49e6ad105d60ac415c1803ab7`.
  - `MD-Reader-v0.10.0-release-unsigned.aab` — **56,709,179 bytes** — SHA-256 `84d0be670af17182a5aa106fa16dcbe8536ad7c1efdac99a60713a9fd00e27f9`.
- رفع Artifact لم ينجح بسبب **GitHub Actions artifact storage quota**؛ GitHub أوضح أن حساب الاستخدام يُعاد احتسابه كل 6–12 ساعة. هذا لا يعني فشل الكود أو البناء، والـCI النهائي نفسه بقي Success بعد أن تم التحقق من APK/AAB والـABI.
- الـAPK الناتج من CI **unsigned**؛ اختبار التحديث على الهاتف يجب أن يستخدم نسخة موقعة بمفتاح الإصدار الثابت نفسه قبل اعتماد v0.10.0.

### ما يجب اختباره على الهاتف قبل الاعتماد
1. فتح ملف بنك الأسئلة الكبير والتمرير السريع في المعاينة.
2. التأكد بصريًا أن `- [x]`, `- [ x ]`, و`-[x]` تظهر كلها بعلامة ✓ وتظليل صحيح.
3. التأكد أن `- [ ]` يبقى اختيارًا فارغًا ولا يُظلل كإجابة صحيحة.
4. تجربة `✅ إجابة صحيحة` و`☐ اختيار غير محدد` و`🧹 توحيد مربعات الاختيار` من أدوات التحرير.
5. التبديل بين معاينة/تحرير في ملف كبير والتأكد من عدم وجود تعليق ملحوظ.
6. اختبار الوضعين الفاتح والداكن، وRTL/LTR.
7. إعادة اختبار الحفظ، Undo/Redo، البحث والاستبدال، Mermaid، الترجمة، الصوت، GitHub Import وعدم حدوث regression.

## 12) قاعدة العمل للإصدار التالي
قبل أي تعديل جديد:
1. قراءة هذا الملف أولًا.
2. إنشاء فرع جديد من `main` المستقر.
3. عدم تغيير package أو signing key.
4. مرشح v0.10.0 الحالي يستخدم `versionCode 13`؛ بعد اعتماده يجب أن يستخدم أي إصدار لاحق `versionCode` أعلى من 13.
5. الحفاظ على `FlingEditText` وسلوك GitHub import المعتمد ما لم يوجد بديل مثبت أفضل.
6. الحفاظ على task normalization/renderer الجديد إذا اجتاز اختبار الهاتف؛ لا نعود للاعتماد على native disabled checkbox وحده.
7. إضافة/تحديث الاختبارات المناسبة.
8. بناء Release والتحقق من الحجم والتوقيع والـABI.
9. اختبار أي UX حساس للمس/الكيبورد/التنزيل على جهاز فعلي.
10. تحديث هذا الملف قبل اعتماد أي إصدار جديد.
11. لا دمج إلى `main` بدون موافقة المستخدم الصريحة.