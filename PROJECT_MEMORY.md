# MD Reader — Project Memory & Source of Truth

> هذا الملف هو المرجع الأساسي للمشروع. يجب تحديثه مع كل تعديل مهم أو قرار معماري أو تغيير في البناء. عند التعارض يرجع إلى أحدث سجل هنا ثم إلى الكود والاختبارات.

## 1) هوية المشروع
- الاسم: **MD Reader**
- النوع: تطبيق Android لقراءة وتحرير Markdown.
- الحزمة الثابتة: `app.mdreader.mobile`
- الإصدار الحالي قيد الاختبار: `0.8.1` — `versionCode 11`.
- الحد الأدنى: Android 8.0 / API 26.
- compile/target SDK: 36.
- الواجهة: عربية RTL مع دعم الإنجليزية LTR.
- الفلسفة: خفيف، محلي قدر الإمكان، بلا إعلانات/تحليلات/تتبع، مع الحفاظ على الوظائف وعدم التضحية بها لمجرد خفض الحجم.

## 2) الوظائف الأساسية المحفوظة
- فتح/تحرير/حفظ ملفات Markdown ووضع معاينة.
- RTL/LTR حسب المحتوى.
- أدوات تنسيق Markdown، التراجع/الإعادة، الجداول، الروابط، الاقتباس، الكود، القوائم وقوائم المهام.
- البحث، الفهرس والتنقل بالعناوين.
- Syntax highlighting وMermaid.
- الملفات الأخيرة والمفضلة واستعادة المسودات.
- حفظ موضع القراءة وحالة التحرير والعلامات المرجعية.
- الترجمة عبر ML Kit والخيارات السابقة.
- القراءة بالصوت المحلي وخيار OpenAI TTS.
- Enter واحد يظهر كسطر جديد في المعاينة (`breaks:true`).
- شريط أدوات التحرير يبقى قابلًا للوصول مع الكيبورد عبر `adjustResize`.
- التنقل السريع داخل المستند وأدوات Markdown الإضافية.
- بحث واستبدال Literal Unicode مع السابق/التالي واستبدال الحالي/الكل.
- تمرير حركي للمحرر بعد رفع الإصبع في v0.8.1.

## 3) قواعد هندسية ثابتة
1. لا تحذف ميزة شغالة لتقليل الحجم أو لتسهيل التنفيذ إلا بقرار موثق.
2. لا توجد سلسلة patch/reconstruction في مسار البناء النهائي. `MainActivity.java` مصدر Java عادي مباشر داخل `app/src/main/java/...`.
3. كل إصدار تسليم يمر ببناء CI، smoke tests، فحص JavaScript، وفحص نواتج APK/AAB.
4. التوزيع الحقيقي يكون Release بمفتاح توقيع ثابت، وليس debug key.
5. الـkeystore وكلمات المرور لا تدخل Git أو `build.gradle`؛ Gradle يقرأ بيانات التوقيع من environment/secrets فقط.
6. ثبات `applicationId` + نفس مفتاح التوقيع + زيادة `versionCode` شرط قبول Android للتحديثات اللاحقة.
7. تقليل الحجم لا يكسر ML Kit أو Mermaid أو الصوت أو أي وظيفة قائمة.
8. تجربة التحرير، خصوصًا التمرير والكيبورد والقوائم، يجب التحقق منها على مستند طويل وعلى جهاز فعلي أيضًا.

## 4) تشخيص الحجم والحل المعتمد
APK v0.7 debug كان تقريبًا 65–67 MB. السبب الأكبر كان ML Kit Translation لأنه ضم مكتبات `libtranslate_jni.so` لأربع معماريات في APK واحد:
- x86_64 ~16.6 MB
- x86 ~16.5 MB
- arm64-v8a ~15.6 MB
- armeabi-v7a ~11.1 MB
- classes.dex ~8.1 MB
- Mermaid asset ~3.2 MB

الحل منذ v0.8:
- Release optimization بواسطة R8 (`minifyEnabled true`).
- `shrinkResources true`.
- APK مباشر خاص بـ `arm64-v8a` بدل حشر جميع ABIs في ملف الهاتف.
- AAB منفصل للنشر بحيث منصة التوزيع تسلم ABI المناسب لكل جهاز.
- ML Kit بقي موجودًا ولم يتم حذفه.

### نتيجة v0.8.0 الفعلية كخط أساس
- ARM64 APK قبل التوقيع: **18,182,663 bytes**.
- ARM64 APK النهائي بعد التوقيع: **18,216,026 bytes** (~17.37 MiB / 18.22 MB decimal).
- AAB قبل التوقيع: **56,693,140 bytes**.
- AAB النهائي بعد التوقيع: **56,720,673 bytes**.
- مقارنةً بـ ~67 MB للـdebug القديم، APK الهاتف أصغر بحوالي **72.8%** تقريبًا.
- فحص محتوى APK أكد وجود `lib/arm64-v8a/libtranslate_jni.so` فقط وعدم وجود x86/x86_64/armeabi-v7a داخله.

## 5) v0.8.0 — ما تم تنفيذه
### أ) البحث والاستبدال
- محرك مستقل: `SearchReplaceEngine.java`.
- Literal Unicode matching، وليس بحث كلمات فقط.
- يقبل العربية والإنجليزية والجمل والمسافات والرموز وعلامات Markdown والإيموجي والنص متعدد الأسطر.
- لا يوجد سقف اصطناعي لعدد المطابقات.
- السابق/التالي.
- استبدال المطابقة الحالية.
- استبدال الكل.
- عداد المطابقات.
- خيار مطابقة حالة الأحرف.
- آمن عندما يحتوي نص الاستبدال على نص البحث نفسه؛ لا يدخل في loop.
- التعديلات تقع على نص المحرر الأصلي القابل للحفظ وتحدّث history/dirty/draft/title.

### ب) استعادة موضع التحرير
- عدم إعادة ضبط موضع المؤشر عند الضغط على تبويب التحرير وهو فعال أصلًا.
- استعادة `selectionStart/selectionEnd/scrollY` لكل مستند.
- تجنب جعل `bringPointIntoView()` يلغي `scrollY` المحفوظ عندما توجد حالة تحرير صريحة.

### ج) تنظيف المعمارية
- المصدر النهائي أصبح مباشرًا في `app/src/main/...`.
- workflow النهائي لا يعيد تركيب `MainActivity` من أجزاء أو patches.
- حذفت workflows الانتقالية بعد انتهاء دورها.
- حذفت `.ci` و`.src-v03` و`.src-parts` وسلسلة patches/source chunks القديمة.
- حذفت أدوات patch القديمة بعد تثبيت المصدر المباشر.
- README تم تحديثه ليتوافق مع المعمارية الحالية.

## 6) نتائج اختبار الهاتف لـ v0.8.0 والمشكلات المكتشفة
تمت تجربة النسخة على جهاز فعلي وظهرت ثلاث مشكلات UX لم تكن اختبارات CI قادرة على كشفها:

### أ) نافذة البحث والاستبدال
- كانت تستدعي `showDialog(..., true)`؛ لذلك كان الـpanel يأخذ **86% من ارتفاع الشاشة** حتى لو كانت عناصره قليلة.
- النتيجة: مساحة فارغة ضخمة أسفل الأزرار بلا فائدة.
- عند ظهور لوحة المفاتيح كانت حقول البحث/الاستبدال قد تتحرك أو تختفي خارج الجزء المرئي.

### ب) قائمة الثلاث نقاط
- `sheet(...)` كان يضيف جميع الأزرار مباشرة داخل `LinearLayout` بلا `ScrollView`.
- عندما تزيد العناصر عن ارتفاع الشاشة يصبح الجزء السفلي غير قابل للوصول.

### ج) سرعة تمرير المحرر
- تنفيذ v0.8.0 اعتمد `AdjustableScroller extends Scroller` وكان يغيّر velocity داخل `Scroller.fling()` فقط.
- اختبار الجهاز أثبت أن مسار اللمس المعتاد داخل `EditText` لم يولّد الـfling المطلوب بالشكل الذي نحتاجه، لذلك تغيير 10%–500% لم ينتج فرقًا عمليًا واضحًا، ورفع الإصبع كان يوقف الحركة.
- هذا التنفيذ أزيل؛ لا نعتبر مجرد وجود slider دليلًا على أن تجربة التمرير صحيحة.

## 7) v0.8.1 — إصلاحات تجربة الهاتف
### أ) تمرير المحرر الحركي الحقيقي
- تمت إضافة `FlingEditText.java` بدل الاعتماد على `AdjustableScroller`.
- `FlingEditText` مبني على Android `OverScroller` + `VelocityTracker`.
- السحب العمودي السريع يمرر النص، وعند رفع الإصبع يبدأ fling حقيقي ويستمر حتى يتباطأ طبيعيًا أو يصل للحافة.
- لمس المحرر مرة أخرى أثناء الحركة يستدعي `stopFling()` ويوقف الانزلاق فورًا.
- نسبة السرعة **10%–500%** تضرب سرعة الـfling الحقيقية بعد رفع الإصبع، لذلك يفترض أن يكون الفرق محسوسًا فعليًا الآن.
- السحب المباشر أثناء ملامسة الإصبع يبقى 1:1؛ النسبة تتحكم أساسًا بالانزلاق الحركي بعد الإفلات.
- السحب العمودي السريع يتحول للتمرير، مع محاولة الحفاظ على سلوك النقر والضغط المطول وتحديد النص الأصلي في `EditText`.
- `AdjustableScroller.java` أزيل بعد استبداله حتى لا يبقى كود ميت أو مساران متعارضان.

### ب) البحث والاستبدال
- نافذة البحث والاستبدال أصبحت `WRAP_CONTENT` بدل panel ثابت بارتفاع 86%.
- حقلا البحث والاستبدال أصبحا مدمجين مبدئيًا بسطر واحد ويمكنهما التمدد حتى 4 أسطر عند النص متعدد الأسطر.
- نافذة الـDialog تضبط `SOFT_INPUT_ADJUST_RESIZE` لكي يعاد قياسها عند ظهور الكيبورد بدل تغطية الحقول قدر الإمكان.
- الهدف: لا مساحة فارغة عملاقة، وحقول الإدخال تبقى قريبة ومرئية أثناء الكتابة.

### ج) قائمة الثلاث نقاط والقوائم الطويلة
- `sheet(...)` يكتشف القوائم الطويلة (`> 7` عناصر).
- القائمة الطويلة توضع داخل `ScrollView` بارتفاع مناسب للشاشة.
- القوائم القصيرة تبقى مدمجة بلا ارتفاع زائد.
- قائمة الثلاث نقاط أصبحت قابلة للتمرير للوصول لكل العناصر السفلية.

## 8) البناء والإصدار
### Gradle الحالي
- `versionName '0.8.1'`.
- `versionCode 11`.
- R8/resource shrinking مفعّلان للـrelease.
- APK الهاتف يبنى مع `-PmdReaderDirectArm64=true`.
- AAB يبنى في Gradle invocation منفصل بدون ABI split.

### لماذا APK وAAB يبنيان منفصلين؟
Android Gradle Plugin رفض الجمع بين multi-APK ABI outputs وApp Bundle مع resource shrinking في build graph واحد بسبب تعدد ملفات `shrunk-resources`. الحل المعتمد فصل العمليتين، وليس تعطيل shrinking أو حذف مكتبة.

### CI لـ v0.8.1
- workflow: `Build MD Reader v0.8.1 Release`.
- يتحقق من وجود `FlingEditText.java` وعدم رجوع MainActivity إلى `AdjustableScroller`.
- يتحقق من `versionCode 11` و`versionName 0.8.1`.
- يتحقق من scrollable action sheet وDialog keyboard resize.
- يشغّل نفس smoke tests السابقة + JavaScript syntax + بناء ARM64 APK + AAB + فحص ABI.
- رقم آخر build نهائي وبصمات التسليم تضاف بعد اكتمال CI والتوقيع.

## 9) التوقيع الدائم
- مفتاح Release ثابت تم إنشاؤه وتسليمه لصاحب المشروع خارج Git.
- النوع الفعلي: PKCS12 داخل ملف `md-reader-release.jks`.
- alias: `mdreader`.
- في PKCS12 الحالي كلمة مرور المفتاح هي نفس كلمة مرور الـkeystore؛ التفاصيل السرية موجودة فقط في حزمة المفتاح الخاصة بصاحب المشروع.
- شهادة SHA-256:
  `3D:7F:96:3D:B1:C9:B5:21:1D:3A:7F:0C:22:7D:20:81:08:47:4B:39:08:02:AF:A6:89:11:33:A0:35:BF:83:5A`
- الصلاحية حتى 2054-01-26.
- لا يُنشأ مفتاح جديد لـ v0.8.1؛ يجب توقيعه بنفس المفتاح حتى يثبت كتحديث فوق v0.8.0 الموقعة.

### قاعدة التحديثات
من ثبت v0.8.0 Release الموقعة بنفس المفتاح يفترض أن يثبت v0.8.1 فوقها كتحديث مباشر لأن `applicationId` ثابت و`versionCode` ارتفع من 10 إلى 11.

## 10) بصمات v0.8.0 المرجعية
- `MD-Reader-v0.8.0-arm64-release.apk`
  - SHA-256: `c21167a87f51b68c99491bba49c69267141abd7d7491f2ffff3a36e370b29de9`
- `MD-Reader-v0.8.0-release.aab`
  - SHA-256: `3ddecc8e802c18317a7d09baf8e2a6ebb204606f98a2bbbc2b1ca792c3b4d4cc`

## 11) معايير قبول v0.8.1
- [x] `MainActivity.java` مصدر مباشر والبناء لا يعتمد على patch chain.
- [x] إزالة تنفيذ `AdjustableScroller` القديم من مسار المحرر.
- [x] إضافة `FlingEditText` مع `OverScroller` و`VelocityTracker`.
- [x] لمس الشاشة يوقف fling الجاري.
- [x] نسبة سرعة التمرير تطبق على velocity الحقيقي للـfling.
- [x] البحث والاستبدال لم يعد يستخدم panel ثابت 86%.
- [x] Dialog البحث يطلب resize مع الكيبورد.
- [x] القوائم الطويلة أصبحت داخل ScrollView.
- [x] `versionCode=11` و`versionName=0.8.1`.
- [ ] CI النهائي clean-head ناجح بعد كل عمليات التنظيف.
- [ ] توقيع APK/AAB بنفس مفتاح Release الثابت والتحقق من البصمة.
- [ ] اختبار فعلي على هاتف المستخدم: fling، إيقافه باللمس، فرق 50/100/200/500، البحث مع الكيبورد، وتمرير قائمة الثلاث نقاط.

## 12) حالة المشروع الآن
- الحالة: **v0.8.1 قيد البناء والتحقق النهائي**.
- فرع العمل: `v0.8-work`.
- PR #2 يبقى Draft وغير مدمج حتى تجربة الهاتف.
- أهم تحقق يدوي بعد صدور APK: ملف Markdown طويل ثم swipe سريع ورفع الإصبع؛ يجب أن يستمر التمرير، ولمسة واحدة يجب أن توقفه.
- لا يدمج الفرع في `main` قبل موافقة المستخدم الصريحة.

## 13) قاعدة العمل للإصدار التالي
قبل أي تعديل جديد:
1. قراءة هذا الملف.
2. عدم تغيير package أو signing key.
3. رفع `versionCode` عند إصدار نسخة جديدة.
4. إضافة الاختبارات المناسبة للميزة.
5. بناء Release والتحقق من الحجم/التوقيع/ABI.
6. اختبار UX الذي يعتمد على اللمس/الكيبورد على جهاز فعلي.
7. تحديث هذا الملف بما تم وما تبقى.
